SET(ICV_INCLUDE_DIRS
	${BRLCAD_BINARY_DIR}/include
	${BRLCAD_SOURCE_DIR}/include
	${BU_INCLUDE_DIRS}
	)
LIST(REMOVE_DUPLICATES ICV_INCLUDE_DIRS)
SET(ICV_INCLUDE_DIRS ${ICV_INCLUDE_DIRS} CACHE STRING "Include directories for libicv" FORCE)
MARK_AS_ADVANCED(ICV_INCLUDE_DIRS)
include_directories(${ICV_INCLUDE_DIRS})

set(LIBICV_SOURCES
    rot.c
)

BRLCAD_ADDLIB(libicv "${LIBICV_SOURCES}" libbu)
SET_TARGET_PROPERTIES(libicv PROPERTIES VERSION 19.0.1 SOVERSION 19)
CMAKEFILES(Makefile.am)
