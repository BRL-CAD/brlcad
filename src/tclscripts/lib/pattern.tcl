# pattern.tcl has been eliminated.
# All procs (pattern_rect, pattern_sph, pattern_cyl, _clone_invoke,
# exists_wrapper, regdef_wrapper) now live in pattern_gui.tcl.
# Dead internal helpers (create_new_name, copy_tree, copy_obj) have
# been removed; their work is performed by libged clone.
