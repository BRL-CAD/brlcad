include_directories(${TERMLIB_INCLUDE_DIR})
#IF(NOT WIN32)
BRLCAD_ADDLIB(libtermio termio.c "${TERMLIB_LIBRARY}")
CMAKEFILES(termio_win32.c)
#ELSE(NOT WIN32)
#BRLCAD_ADDLIB(libtermio termio_win32.c "${TERMLIB_LIBRARY}")
#CMAKEFILES(termio.c)
#ENDIF(NOT WIN32)
SET_TARGET_PROPERTIES(libtermio PROPERTIES VERSION 19.0.1 SOVERSION 19)
