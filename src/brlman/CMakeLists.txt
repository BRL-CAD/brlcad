SET(BRLCAD_ROOT ${BRLCAD_PREFIX})
SET(BRLCAD_DATA ${BRLCAD_INSTALL_DATA_DIR})
SET(prefix ${BRLCAD_PREFIX})
SET(datarootdir ${BRLCAD_INSTALL_DATA_DIR})
SET(mandir "${BRLCAD_INSTALL_MAN_DIR}")
SET(PAGER "\${PAGER}")
SET(MANPAGER "\${MANPAGER}")
SET(MANPATH "\${MANPATH}")
SET(path_to_this "\${path_to_this}")
SET(path_elements "\${path_elements}")
SET(dir "\${dir}")
SET(pattern "\${pattern}")
SET(try "\${try}")

configure_file(brlman.sh.in ${CMAKE_BINARY_DIR}/src/brlman/brlman)
INSTALL(PROGRAMS ${CMAKE_BINARY_DIR}/src/brlman/brlman DESTINATION ${BRLCAD_INSTALL_BIN_DIR})
install(FILES brlman.1 DESTINATION ${BRLCAD_INSTALL_MAN_DIR}/man1)
