set(LIBORLE_SOURCES
    rle.c
)

include_directories(
    ${UTAHRLE_INCLUDE_DIR}
)

BRLCAD_ADDLIB(liborle "${LIBORLE_SOURCES}" "NONE" STRICT)
SET_TARGET_PROPERTIES(liborle PROPERTIES VERSION 19.0.1 SOVERSION 19)

install(FILES liborle.3 DESTINATION ${MAN_DIR}/man3)
