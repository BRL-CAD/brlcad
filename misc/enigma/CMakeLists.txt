include_directories(
  ${BRLCAD_BINARY_DIR}/include
  ${BRLCAD_SOURCE_DIR}/include
)

add_definitions(
  -DHAVE_CONFIG_H
  -DBRLCADBUILD
)
IF(CRYPT_LIBRARY)
	BRLCAD_ADDEXEC(enigma enigma.c "${BSD_LIBRARY} ${CRYPT_LIBRARY}")
ELSE(CRYPT_LIBRARY)
	BRLCAD_ADDEXEC(enigma "enigma.c crypt.c" "${BSD_LIBRARY}")
ENDIF(CRYPT_LIBRARY)
INSTALL(FILES enigma.1 DESTINATION ${BRLCAD_INSTALL_MAN_DIR}/man1)
