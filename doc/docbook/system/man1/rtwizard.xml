<refentry xmlns="http://docbook.org/ns/docbook" version="5.0" xml:id="rtwizard1">
  <refmeta>
    <refentrytitle>RTWIZARD
    </refentrytitle>
    <manvolnum>1</manvolnum>
    <refmiscinfo class="source">BRL-CAD</refmiscinfo>
    <refmiscinfo class="manual">BRL-CAD User Commands</refmiscinfo>
  </refmeta>

  <refnamediv xml:id="rtwizard_name">
    <refname>rtwizard</refname>
    <refpurpose>Combines images from several raytracing techniques to produce complex images.</refpurpose>
  </refnamediv>

  <!-- body begins here -->
  <refsynopsisdiv xml:id="rtwizard_synopsis">
    <cmdsynopsis sepchar=" ">
      <command>rtwizard</command>
      <arg choice="opt" rep="repeat"><replaceable>options</replaceable></arg>
    </cmdsynopsis>
  </refsynopsisdiv>

  <refsection xml:id="rtwizard_description">
    <title>DESCRIPTION</title>

    <para>
      <emphasis remap="I">RtWizard</emphasis> is a tool that
      orchestrates several BRL-CAD utilities to produce complex images
      of geometry in prescribed styles.  There are two primary modes
      of operation.
    </para>

    <para>
      The first mode of operation is a graphical client to guide users
      through the process of creating images interactively.  To invoke
      that mode, simply run <command>rtwizard</command> and a
      wizard-style GUI should appear.  Graphical mode has integrated
      documentation and workflows, and is intended to be discoverable
      and self-explanatory.
    </para>

    <para>
      The second mode of operation is a command line mode to allow
      users who already know what options they wish to use to quickly
      and automatically produce images.  It is also useful for
      programmatic scripting and bulk rendering of images.
    </para>

    <para>
      <emphasis remap="I">RtWizard</emphasis> generates and composites
      images in a variety of styles including full-color images, edge
      line drawings, and "ghosted" images with options for
      highlighting and emphasizing model features and subsets of
      models.  Color ray tracings are produced by
      <command>rt</command>, "hidden-line drawings" produced by
      <command>rtedge</command>, and semi-transparent / faded
      greyscale "ghosted" ray tracings are produced with
      <command>rt</command> and a variety of image manipulation tools
      included with BRL-CAD.  GUI-mode presents workflows for setting
      up each image type, and command-line mode provides the
      <option>--type</option>, described below.
    </para>

    <para>
      The following command-line options are recognized by
      <command>rtwizard</command>:
    </para>
    <variablelist remap="TP">
      <title>Input/Output Options</title>

      <varlistentry>
        <term><option>-i</option> <replaceable>geometry_file</replaceable></term>
        <term><option>--input</option> <replaceable>geometry_file</replaceable></term>
        <listitem>
          <para>
	    Specifying the geometry database containing the objects to
	    be raytraced.  The <option>i</option> option will override
	    a non-flagged geometry file specification.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-o</option> <replaceable>output_file[.imgformat]</replaceable></term>
        <term><option>--output</option> <replaceable>output_file[.imgformat]</replaceable></term>
        <listitem>
          <para>
	    The name (and optionally the output image format) of the
	    final image generated by rtwizard.  If the output filename
	    uses a .png file extension, the Portable Network Graphics
	    image format will be used when writing out the file.
	    Otherwise, a standard BRL-CAD pix image will be generated.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-d</option> <replaceable>framebuffer_device</replaceable></term>
        <term><option>--fbserv-device</option> <replaceable>framebuffer_device</replaceable></term>
        <listitem>
          <para>
            Under the hood, <command>fbserv</command> is run to assist
            with processing.  The <option>-d</option> option specifies
            the type of framebuffer <command>fbserv</command> should
            use in lieu of the default memory-only interface
            (/dev/mem).  See <command>fbhelp</command> for a listing
            of available device options. Framebuffer servers started
            by <command>rtwizard</command> are automatically closed
            after image generation is complete.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-p</option> <replaceable>port_number</replaceable></term>
        <term><option>--fbserv-port</option> <replaceable>port_number</replaceable></term>
        <listitem>
          <para>
	    The <option>-p</option> option specifies the framebuffer
	    port number to use when running <command>fbserv</command>
	    to establish a framebuffer.  An existing framebuffer
	    server port be specified.  In the case of a pre-existing
	    framebuffer servers, <command>rtwizard</command> will not
	    close the framebuffer automatically after the raytracing
	    process is complete.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>--gui</option></term>
        <listitem>
          <para>
	    Run rtwizard's graphical interface, even if the command
	    line options specify enough information to produce an
	    image.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>--no-gui</option></term>
        <listitem>
          <para>
	    Run rtwizard on the command line, even if the command line
	    options do not specify enough information to produce an
	    image.  Overridden by --gui option if both are specified.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>

    <variablelist remap="TP">
      <title>Model View Options</title>
      <para>
        When it comes to preparing the view for an <emphasis
        remap="I">RtWizard</emphasis> image, there are two options.
        One is to use the "user level" controls, which reflect the
        view manipulations used by users on the command line.  The
        other is to use "low level" specifiers, which are more
        typically used when <command>rtwizard</command> is run by
        automated scripts.  The two methods are mutually exclusive.
      </para>
      <varlistentry>
        <term><option>-a</option> <replaceable>angle</replaceable></term>
        <term><option>--azimuth</option> <replaceable>angle)</replaceable></term>
        <listitem>
          <para>Set azimuth angle (in degrees).</para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-e</option> <replaceable>angle</replaceable></term>
        <term><option>--elevation</option> <replaceable>angle</replaceable></term>
        <listitem>
          <para>
            Set elevation angle (in degrees).
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>--twist</option> <replaceable>angle</replaceable></term>
        <listitem>
          <para>
            Set twist angle (in degrees).
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-P</option> <replaceable>angle</replaceable></term>
        <term><option>--perspective</option> <replaceable>angle</replaceable></term>
        <listitem>
          <para>
            Set the perspective angle (in degrees).
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-z</option> <replaceable>value</replaceable></term>
        <term><option>--zoom</option> <replaceable>value</replaceable></term>
        <listitem>
          <para>
            Set the zoom factor, specified as a multiplier on the
            default view size. Values less than one result in a
            smaller object in the view.  Values greater than one
            result in larger objects (due to the view being zoomed).
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>--center</option> <replaceable>X Y Z</replaceable> </term>
        <listitem>
          <para>
            Set the xyz coordinates of the center of the view.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>

    <variablelist remap="TP">
      <title>Image Generation Options</title>

      <varlistentry>
        <term><option>-c</option> <replaceable>obj1[,...]</replaceable> </term>
        <term><option>--color-objects</option> <replaceable>obj1[,...]</replaceable> </term>
        <listitem>
          <para>
            Specify objects to render using full color using a
            comma-separated list.  The entire list may be enclosed in
            double-quotes if object names have spaces (e.g., -c
            "obj1,obj 2").
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-C</option> <replaceable>value</replaceable></term>
        <term><option>--background-color</option> <replaceable>value</replaceable></term>
        <listitem>
          <para>
            Set the background color to use when raytracing full color
            objects.  May use R/G/B or hex style color specification.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-g</option> <replaceable>obj1[,...]</replaceable></term>
        <term><option>--ghost-objects</option> <replaceable>obj1[,...]</replaceable></term>
        <listitem>
          <para>
            Specify objects to be rendered using ghosting using a
            comma-separated list.  The entire list may be enclosed in
            double-quotes if object names have spaces (e.g., -g
            "obj1,obj 2").
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-G</option> <replaceable>value</replaceable></term>
        <term><option>--ghosting-intensity</option> <replaceable>value</replaceable></term>
        <listitem>
          <para>
            Control the degree of visibility to use when rendering
            ghost objects.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><option>-l</option> <replaceable>obj1[,...]</replaceable></term>
        <term><option>--line-objects</option> <replaceable>obj1[,...]</replaceable></term>
        <listitem>
          <para>
            Specify objects to render edge lines for using a
            comma-separated list.  The entire list may be enclosed in
            double-quotes if object names have spaces (e.g., -l
            "obj1,obj 2").
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>--line-color</option> <replaceable>value</replaceable></term>
        <listitem>
          <para>
            Specify color to use when rendering edge lines.  In
            addition to R/G/B and hex color specifications, the
            keyword "region" is also supported - in the latter case,
            region colors will be used for lines.
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>--non-line-color</option> <replaceable>value</replaceable></term>
        <listitem>
          <para>
            Specify the color to use for non-line rendering.
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-n</option>
        <replaceable>value</replaceable></term>
        <term><option>--height</option> <replaceable>value</replaceable></term>
        <listitem>
          <para>
            Specify the height of the generated image in pixels.
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-O</option> <replaceable>value</replaceable></term>
        <term><option>--occlusion</option> <replaceable>value</replaceable></term>
        <listitem>
          <para>
            Specify the occlusion mode to use for line rendering. See
            <command>rtedge</command> manual page for value options.
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-s</option> <replaceable>value</replaceable></term>
        <term><option>--size</option> <replaceable>value</replaceable></term>
        <listitem>
          <para>
            Specify the width and height of the generated image in
            pixels.  Width can be overridden by the <option>w</option>
            option and height by the <option>n</option> option.
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-t</option> <replaceable>image_type</replaceable></term>
        <term><option>--type</option> <replaceable>image_type</replaceable></term>
        <listitem>
          <para>
            Specify the type of image to be rendered. The images
            produced by <command>rtwizard</command> are categorized by
            <emphasis remap="I">picture type</emphasis>.  There are
            six picture types built on full color raytracing,
            ghosting, and edge line renderings.
          </para>
          <para>
            NOTE: If a type is specified and insufficient information
            is supplied to generate that particular image type,
            rtwizard will exit with an error.
          </para>

          <para>
            <table>
              <title>RtWizard Image Types</title>

              <tgroup cols="3">
	        <thead>
	          <row>
	            <entry>Type</entry>
	            <entry>Name</entry>
	            <entry>Description</entry>
	          </row>
	        </thead>
	        <tbody>
	          <row>
	            <entry>A</entry>
	            <entry>Simple Full-Color Image</entry>
	            <entry>Standard <command>rt</command>
	            image.</entry>
	          </row>
	          <row>
	            <entry>B</entry>
	            <entry>Simple Line Drawing</entry>
	            <entry>Standard <command>rtedge</command>
	            image.</entry>
	          </row>
	          <row>
	            <entry>C</entry>
	            <entry>Highlighted Image</entry>
	            <entry>Full Color <command>rt</command> image
	            enhanced with <command>rtedge</command>
	            lines.</entry>
	          </row>
	          <row>
	            <entry>D</entry>
	            <entry>Mixed Full Color and Edges</entry>
	            <entry>Like Type C, except objects may be
	            selectively enhanced with
	            <command>rtedge</command> lines.</entry>
	          </row>
	          <row>
	            <entry>E</entry>
	            <entry>Ghost Image with Inserts</entry>
	            <entry>A combination of Full Color elements and
	            faded greyscale raytracings for context.</entry>
	          </row>
	          <row>
	            <entry>F</entry>
	            <entry>Ghost Image with Inserts and Edges</entry>
	            <entry>A Type E image further enhanced with
	            <command>rtedge</command> lines.</entry>
	          </row>
	        </tbody>
              </tgroup>
            </table>
          </para>
        </listitem>
      </varlistentry>

      <varlistentry>
        <term><option>-w</option> <replaceable>value</replaceable></term>
        <term><option>--width</option> <replaceable>value</replaceable></term>
        <listitem>
          <para>
            Specify the width of the generated image in pixels.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>

    <variablelist remap="TP">
      <title>Compatibility Options</title>

      <varlistentry>
        <term><replaceable>geometry_file.g</replaceable></term>
        <listitem>
          <para>
            For usage compatibility with <command>rt</command> and
            other tools, the first (leftmost) argument without an
            option flag that identifies an existing .g file is assumed
            to be the input geometry database.  Note, some argument
            configurations can result in ambiguity (e.g., objects with
            a .g suffix).  As such, The <option>-i</option> is
            preferred for specifying the input geometry database.
          </para>
        </listitem>
      </varlistentry>
      <varlistentry>
        <term><replaceable>output_image.{pix|png}</replaceable></term>
        <listitem>
          <para>
	    This specifies the file name and image format of the
	    resulting image to be output by rtwizard.  When specifying
	    the output file name without using a flag, a .pix or .png
	    image format extension is necessary.  The first valid,
	    unflagged output file name will be used unless overridden
	    by the <option>-o</option>.  Note that some option flags
	    accept multiple arguments and can create ambiguity. In
	    such situations <option>-o</option> should be used.
          </para>
        </listitem>
      </varlistentry>
    </variablelist>

  </refsection>

  <refsection xml:id="examples">
    <title>EXAMPLES</title>

    <para>
      Introduction
    </para>

    <example>
      <title>Simple Full-Color Image</title>

      <para>
        <userinput>rtwizard -i m35.g -c component -o m35.png</userinput>
      </para>
      <para>
        Results in a default color image of the m35 truck object named
        'component' with output saved to m35.png
      </para>
    </example>

    <example>
      <title>Simple Line Drawing Image</title>

      <para>
        <userinput>rtwizard -d /dev/wgl -i m35.g -l component</userinput>
      </para>
      <para>
        Results in a line drawing of the m35 truck being display in a
        Windows-specific OpenGL window instead of saving to a file.
      </para>
    </example>

    <example>
      <title>Ghost Image with Inserts and Edges</title>

      <para>
        <userinput>rtwizard -i m35.g -c component/power.train -g component -l component</userinput>
      </para>
      <para>
        Results in a view of the m35 truck highlighting the engine (in
        color) with the truck being shown as a ghosted, edged
        background, with output saved to a default rtwizard.pix file.
      </para>
    </example>

    <example>
      <title>Complex Image with Non-Default View</title>

      <para>
        <userinput>rtwizard -i m35.g -c component/power.train -g component -l component -a -35 -e 15 -z 1.6</userinput>
      </para>
      <para>
        Same as the previous image, except viewing the truck from a
        different direction and zoomed in closer, with output saved to
        a default rtwizard.pix file.
      </para>
    </example>

    <example>
      <title>Multiple Color Objects</title>

      <para>
        <userinput>rtwizard -d /dev/ogl -i m35.g -c component/power.train,component/suspension -z 1.6</userinput>
      </para>
      <para>
        View the power train and suspension of the truck as color
        objects in a Mac-/Linux-specific OpenGL window.
      </para>
    </example>

  </refsection>

  <refsection xml:id="see_also">
    <title>SEE ALSO</title>

    <para><citerefentry><refentrytitle>rt</refentrytitle><manvolnum>1</manvolnum></citerefentry>,<citerefentry><refentrytitle>rtedge</refentrytitle><manvolnum>1</manvolnum></citerefentry>,<citerefentry><refentrytitle>fbhelp</refentrytitle><manvolnum>1</manvolnum></citerefentry>,<citerefentry><refentrytitle>fbserv</refentrytitle><manvolnum>1</manvolnum></citerefentry></para>
  </refsection>

  <refsection xml:id="copyright">
    <title>COPYRIGHT</title>

    <para>This software is Copyright (c) 2001-2023 United States
    Government as represented by the U.S. Army Research
    Laboratory.</para>
  </refsection>

  <refsection xml:id="author">
    <title>AUTHOR</title>
    <para>BRL-CAD Team</para>
  </refsection>

  <refsection xml:id="bug_reports">
    <title>BUG REPORTS</title>

    <para>Reports of bugs or problems should be submitted via
    electronic mail to <email>devs@brlcad.org</email></para>
  </refsection>
</refentry>
