<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='getx111'>
<!--  Copyright (c) 1988, University of Utah -->
<refmeta>
<refentrytitle>GETX11</refentrytitle>
<manvolnum>1</manvolnum>
<refmiscinfo class='source'>Jan 28, 1990</refmiscinfo>
<refmiscinfo class='manual'>1</refmiscinfo>
</refmeta>
<refnamediv>
<refname>getx11</refname>
<refpurpose>get RLE images to an X11 display</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<cmdsynopsis>
  <command>getx11</command>    
    <arg choice='opt'><arg choice='plain'>- </arg><arg choice='plain'><replaceable>window_geometry</replaceable></arg></arg>
    <arg choice='opt'>-a </arg>
    <arg choice='opt'>-d <replaceable>display</replaceable></arg>
    <arg choice='opt'>-D </arg>
    <arg choice='opt'>-f </arg>
    <arg choice='opt'>-g <replaceable>display_gamma</replaceable></arg>
    <arg choice='opt'><arg choice='plain'>- </arg><arg choice='opt'><replaceable>iI</replaceable></arg><arg choice='plain'><replaceable>image_gamma</replaceable></arg></arg>
    <arg choice='opt'>-j </arg>
    <arg choice='opt'><arg choice='plain'>-m </arg><arg choice='opt'><replaceable>maxframes/sec</replaceable></arg></arg>
    <arg choice='opt'>-n <replaceable>levels</replaceable></arg>
    <arg choice='opt'>-s </arg>
    <arg choice='opt'>-t <replaceable>title</replaceable></arg>
    <arg choice='opt'>-v </arg>
    <arg choice='opt'><arg choice='plain'>- </arg><arg choice='opt'><replaceable>wW</replaceable></arg></arg>
    <arg choice='opt'>-x <replaceable>visualtype</replaceable></arg>
    <arg choice='opt' rep='repeat'><replaceable>infile</replaceable></arg>
</cmdsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para>This program displays an
<citerefentry><refentrytitle>RLE</refentrytitle><manvolnum>5</manvolnum></citerefentry>
file on an
<emphasis remap='I'>X11</emphasis>
display.  It uses a dithering technique to take a
full-color or gray scale image into the limited number of colors
typically available under
<emphasis remap='I'>X</emphasis>.
Its default behavior is to try to
display the image in color with as many brightness levels as possible
(except on a one bit deep display).  Several
<command>getx11</command>
processes running simultaneously with the same color resolution will
share color map entries.</para>

<para><emphasis remap='I'>Getx11</emphasis>
uses the standard
<emphasis remap='I'>X</emphasis>
window creation procedure to create a window with a location and size
specified by the user, with the restriction that the window must be at
least as large as the input image.  If the window is turned into an
icon, a smaller version of the image will be displayed in the icon.</para>

<para>If the input image has only a single channel, and has a color map, then
this color map will be loaded directly (if possible) instead of using
the normal dithering process.  Many images will look better if
pre-processed by
<citerefentry><refentrytitle>mcut</refentrytitle><manvolnum>1</manvolnum></citerefentry>
or
<citerefentry><refentrytitle>rlequant</refentrytitle><manvolnum>1</manvolnum></citerefentry>,
both of which produce images reduced to a single channel with a
colormap.  This is because the colors that are used to display the
image are chosen to be a good set of colors for that particular image,
rather than a set of colors that are mediocre for all images.  The
color map so created will not be shared with other windows.  The
picture comment <emphasis remap='I'>colormap_length</emphasis> specifies the exact number of
useful entries in the input color map.  If this is significantly less
than 256, this can save space in the shared X color map.</para>
</refsect1>

<refsect1 xml:id='options'><title>OPTIONS</title>
<variablelist remap='TP'>
  <varlistentry>
  <term><option>-=</option><replaceable> window_geometry</replaceable></term>
  <listitem>
<para>Specify the geometry of the window in which the image will be
displayed.  This is useful mostly for giving the location of the
window, as the size of the window will be at least as large as the
size of the image.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-a</option></term>
  <listitem>
<para>"As is", suppress dithering.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-d</option><replaceable> display</replaceable></term>
  <listitem>
<para>Give the name of the
<emphasis remap='I'>X</emphasis>
display to display the image on.  Defaults to the value of the
environment variable
<emphasis remap='I'>DISPLAY</emphasis>.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-D</option></term>
  <listitem>
<para>"Debug mode".  The operations in the input
<citerefentry><refentrytitle>RLE</refentrytitle><manvolnum>5</manvolnum></citerefentry>
file will be printed as they are read.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-f</option></term>
  <listitem>
<para>"No fork."  Normally,
<command>getx11</command>
will fork itself after putting the image on the screen, so that the
parent process may return the shell, leaving an "invisible" child to
keep the image refreshed.  If
<option>-f</option>
is specified, getx11 will not exit to the shell until the image is removed.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-g</option><replaceable> display_gamma</replaceable></term>
  <listitem>
<para>Specify the gamma of the
<emphasis remap='I'>X</emphasis>
display monitor.  The default value is 2.5, suitable for most color TV
monitors (this is the gamma value assumed by the NTSC video standard).</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-i</option><replaceable> image_gamma</replaceable></term>
  <listitem>
<para>Specify the gamma (contrast) of the image.  A low contrast image,
suited for direct display without compensation on a high contrast
monitor (as most monitors are) will have a gamma of less than one.
The default image gamma is 1.0.  Image gamma may also be specified by
a picture comment in the
<emphasis remap='I'>RLE (5)</emphasis>
file of the form
<emphasis remap='B'>image_gamma=</emphasis><emphasis remap='I'>gamma.</emphasis>
The command line argument will override the value in the file if specified.
The dithering process assumes that the incoming image has a gamma of
1.0 (i.e., a 200 in the input represents an intensity twice that of
a 100.)  If this is not the case, the input values must be adjusted
before dithering.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-I</option><replaceable> image_gamma</replaceable></term>
  <listitem>
<para>An alternate method of specifying the image gamma, the number
following
<option>-I</option>
is the gamma of the display for which the image was originally
computed (and is therefore 1.0 divided by the actual gamma of the
image).  Image display gamma may also be specified by
a picture comment in the
<emphasis remap='I'>RLE (5)</emphasis>
file of the form
<emphasis remap='B'>display_gamma=</emphasis><emphasis remap='I'>gamma.</emphasis>
The command line argument will override the value in the file if specified.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-j</option></term>
  <listitem>
<para>"Jump mode".  When reading an image from the standard input, each scan
line is normally displayed as soon as it is read.  This allows a user
to monitor the progress of an image generating program, for example
(common usage is "tail -f image.rle | getx11").  Images read directly
from files are only updated after every 10 lines are read to improve
the display speed.  This behavior can be forced for the standard input
by specifying jump mode.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-m</option> [ <emphasis remap='I'>maxframes/sec</emphasis> ]</term>
  <listitem>
<para>"Movie mode."  Optional argument is maximum rate at which movies will play, in
frames per second.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-n</option><replaceable> levels</replaceable></term>
  <listitem>
<para>Specify the number of gray or color levels to be used in the dithering
process.  If not this many levels are available,
<command>getx11</command>
will try successively fewer levels until it is able to allocate enough
color map entries.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-s</option></term>
  <listitem>
<para>"Stingy mode".  Normally,
<command>getx11</command>
allocates an X server pixmap for each image to speed up the window
refresh.  If many images are displayed, the server may run out of
memory to store these pixmaps (or its virtual memory size may get very
large).  Stingy mode suppresses pixmap allocation (except in movie
mode, where the pixmaps are necessary for reasonable performance).</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-t</option><replaceable> title</replaceable></term>
  <listitem>
<para>The window name for an image window normally comes from the input file
name or a
<emphasis remap='B'>image_title=</emphasis><emphasis remap='I'>title</emphasis>
comment in the RLE file.  The window name can be forced to a
particular string with this option.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-v</option></term>
  <listitem>
<para>Verbose.  (But less so than with
<option>-D.)</option></para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-w</option></term>
  <listitem>
<para>This flag forces
<command>getx11</command>
to produce a gray scale (black-and-white) dithered image instead
of a color image.
Color input will be transformed to black and white via the
<emphasis remap='I'>NTSC Y</emphasis>
transform.  On a low color resolution display (a display with only 4
bits, for example), this will produce a much smoother looking image
than color dithering.  It may be used in conjunction with
<option>-n</option>
to produce an image with a specified number of gray levels.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-W</option></term>
  <listitem>
<para>This flag forces
<command>getx11</command>
to display the image as a bitonal black and white bitmap image.  This is the
only mode available on monochrome (non gray scale) displays (and is
the default there).  Black pixels will be displayed using the
<citerefentry><refentrytitle>BlackPixel</refentrytitle><manvolnum>3X</manvolnum></citerefentry>
value and white with the
<citerefentry><refentrytitle>WhitePixel</refentrytitle><manvolnum>3X</manvolnum></citerefentry>
value (note that these may not be black and
white on certain displays, or when they have been modified by the user.)</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-x</option><replaceable> visual_type</replaceable></term>
  <listitem>
<para>Specify X visual type to be used.  The value may be a string or a number.
This number is assumed to be an integer
between 0 and 5, denoting
<citerefentry><refentrytitle>staticgray</refentrytitle><manvolnum>0</manvolnum></citerefentry>,<citerefentry><refentrytitle>grayscale</refentrytitle><manvolnum>1</manvolnum></citerefentry>,
<citerefentry><refentrytitle>pseudocolor</refentrytitle><manvolnum>2</manvolnum></citerefentry>,<citerefentry><refentrytitle>staticcolor</refentrytitle><manvolnum>3</manvolnum></citerefentry>,
<citerefentry><refentrytitle>truecolor</refentrytitle><manvolnum>4</manvolnum></citerefentry>,
or
<citerefentry><refentrytitle>directcolor</refentrytitle><manvolnum>5</manvolnum></citerefentry>.
The string must match one of these visual types
(any capitalization is ignored).</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='I'>infile ...</emphasis></term>
  <listitem>
<para>Name(s) of the
<citerefentry><refentrytitle>RLE</refentrytitle><manvolnum>5</manvolnum></citerefentry>
file(s) to display.  If not specified, the image will be read from the
standard input.  In movie mode, you get one window, and zooming
is disabled.  In normal mode, you get one window per image.</para>
  </listitem>
  </varlistentry>
</variablelist>
</refsect1>

<refsect1 xml:id='mousekey_actions'><title>Mouse/key actions (normal mode)</title>
<variablelist remap='TP'>
  <varlistentry>
  <term>Mouse 1 (left):</term>
  <listitem>
<para>Increase zoom factor by 1, center on this pixel.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Mouse 2 (middle):</term>
  <listitem>
<para>Recenter on this pixel.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Mouse 3 (right):</term>
  <listitem>
<para>Decrease zoom factor by 1, center on this pixel.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Shift mouse 1:</term>
  <listitem>
<para>Show value at this pixel.  In B&amp;W, just shows intensity.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Shift mouse 2:</term>
  <listitem>
<para>Toggle between zoomed and unzoomed.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>q,Q,^C:</term>
  <listitem>
<para>Quit.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>1,2,3,4,5,6,7,8,9:</term>
  <listitem>
<para>Set zoom factor.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Arrow keys:</term>
  <listitem>
<para>Move image (when zoomed).  Shifted moves faster.</para>
  </listitem>
  </varlistentry>
</variablelist>
</refsect1>

<refsect1 xml:id='mousekey_actions2'><title>Mouse/key actions (movie mode)</title>
<variablelist remap='TP'>
  <varlistentry>
  <term>Mouse 1:</term>
  <listitem>
<para>Run movie forward.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Shift Mouse 1:</term>
  <listitem>
<para>Run movie continuously in current direction.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Mouse 2:</term>
  <listitem>
<para>Step movie one frame in current direction.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Shift Mouse 2:</term>
  <listitem>
<para>Set movie speed by moving mouse "up" and "down".  The speed chosen is displayed
in the upper right corner of the window.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Mouse 3:</term>
  <listitem>
<para>Run movie backward.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>space:</term>
  <listitem>
<para>Flip one frame in current direction.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>b:</term>
  <listitem>
<para>"Bounce" image - run it continuously forwards, then backwards, then
forwards, ...</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>c,C:</term>
  <listitem>
<para>Run move continuously.  "c" runs it forward, "C" runs it backward.
When the movie reaches the "end", it will immediately restart from the
beginning.</para>
  </listitem>
  </varlistentry>
</variablelist>

<para>All continuing movie action can be halted by pressing a key or mouse button.</para>
</refsect1>

<refsect1 xml:id='see_also'><title>SEE ALSO</title>
<para><citerefentry><refentrytitle>urt</refentrytitle><manvolnum>1</manvolnum></citerefentry>,
<citerefentry><refentrytitle>RLE</refentrytitle><manvolnum>5</manvolnum></citerefentry>.</para>
</refsect1>

<refsect1 xml:id='author'><title>AUTHOR</title>
<para>Spencer W. Thomas, University of Utah (X10 version)</para>

<para>Andrew F. Vesper, Digital Equipment Corp. (X11 modifications)</para>

<para>Martin R. Friedmann, University of Michigan (better X11, flipbook,
magnification, info)</para>
</refsect1>

<refsect1 xml:id='bugs'><title>BUGS</title>
<para>Display to a 24-bit visual is somewhat optimized, but could be faster.</para>

<para>Doesn't pay any attention to the X resource database (i.e., cannot be
customized via the
<markup>.Xdefaults</markup>
file).  The options, while standard for the raster toolkit, are
non-standard for X.</para>
</refsect1>
</refentry>

