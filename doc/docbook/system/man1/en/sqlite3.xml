<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='sqlite31'>
<!--                                       Hey, EMACS: \-*\- nroff \-*\-
 First parameter, NAME, should be all caps
 Second parameter, SECTION, should be 1\-8, maybe w/ subsection
 other parameters are allowed: see man(7), man(1) -->
<refmeta>
<refentrytitle>SQLITE3</refentrytitle>
<manvolnum>1</manvolnum>
<refmiscinfo class='source'>Mon Apr 15 23:49:17 2002</refmiscinfo>
</refmeta>
<refnamediv>
<refname></refname>
<refpurpose>A command line interface for SQLite version 3</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<cmdsynopsis>
  <command>sqlite3</command>    
    <arg choice='opt'><replaceable>options</replaceable></arg>
    <arg choice='opt'><replaceable>databasefile</replaceable></arg>
    <arg choice='opt'><replaceable>SQL</replaceable></arg>
    <sbr/>
</cmdsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='summary'><title>SUMMARY</title>
<para><command>sqlite3</command>
is a terminal-based front-end to the SQLite library that can evaluate
queries interactively and display the results in multiple formats.
<command>sqlite3</command>
can also be used within shell scripts and other applications to provide
batch processing features.</para>

</refsect1>

<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para>To start a
<command>sqlite3</command>
interactive session, invoke the
<command>sqlite3</command>
command and optionally provide the name of a database file.  If the
database file does not exist, it will be created.  If the database file
does exist, it will be opened.</para>

<para>For example, to create a new database file named "mydata.db", create
a table named "memos" and insert a couple of records into that table:</para>

<para>$ 
<emphasis remap='B'>sqlite3 mydata.db</emphasis>
<!-- .br -->
SQLite version 3.1.3
<!-- .br -->
Enter ".help" for instructions
<!-- .br -->
sqlite&gt;
<emphasis remap='B'>create table memos(text, priority INTEGER);</emphasis>
<!-- .br -->
sqlite&gt;
<emphasis remap='B'>insert into memos values('deliver project description', 10);</emphasis>
<!-- .br -->
sqlite&gt;
<emphasis remap='B'>insert into memos values('lunch with Christine', 100);</emphasis>
<!-- .br -->
sqlite&gt;
<emphasis remap='B'>select * from memos;</emphasis>
<!-- .br -->
deliver project description|10
<!-- .br -->
lunch with Christine|100
<!-- .br -->
sqlite&gt;</para>


<para>If no database name is supplied, the ATTACH sql command can be used
to attach to existing or create new database files.  ATTACH can also
be used to attach to multiple databases within the same interactive
session.  This is useful for migrating data between databases,
possibly changing the schema along the way.</para>

<para>Optionally, a SQL statement or set of SQL statements can be supplied as
a single argument.  Multiple statements should be separated by
semi-colons.</para>

<para>For example:</para>

<para>$ 
<userinput>sqlite3 -line mydata.db 'select * from memos where priority &gt; 20;'</userinput>
<!-- .br -->
    text = lunch with Christine
<!-- .br -->
priority = 100</para>
<!-- .br -->



<refsect2 xml:id='sqlite_metacommands'><title>SQLITE META-COMMANDS</title>

<para>The interactive interpreter offers a set of meta-commands that can be
used to control the output format, examine the currently attached
database files, or perform administrative operations upon the
attached databases (such as rebuilding indices).   Meta-commands are
always prefixed with a dot (.).</para>

<para>A list of available meta-commands can be viewed at any time by issuing
the '.help' command.  For example:</para>

<para>sqlite&gt;
<markup>.help</markup></para>
<literallayout remap='.nf'>
.databases             List names and files of attached databases
.dump ?TABLE? ...      Dump the database in an SQL text format
.echo ON|OFF           Turn command echo on or off
.exit                  Exit this program
.explain ON|OFF        Turn output mode suitable for EXPLAIN on or off.
.header(s) ON|OFF      Turn display of headers on or off
.help                  Show this message
.import FILE TABLE     Import data from FILE into TABLE
.indices TABLE         Show names of all indices on TABLE
.mode MODE ?TABLE?     Set output mode where MODE is one of:
                         csv      Comma-separated values
                         column   Left-aligned columns.  (See .width)
                         html     HTML &lt;table&gt; code
                         insert   SQL insert statements for TABLE
                         line     One value per line
                         list     Values delimited by .separator string
                         tabs     Tab-separated values
                         tcl      TCL list elements
.nullvalue STRING      Print STRING in place of NULL values
.output FILENAME       Send output to FILENAME
.output stdout         Send output to the screen
.prompt MAIN CONTINUE  Replace the standard prompts
.quit                  Exit this program
.read FILENAME         Execute SQL in FILENAME
.schema ?TABLE?        Show the CREATE statements
.separator STRING      Change separator used by output mode and .import
.show                  Show the current values for various settings
.tables ?PATTERN?      List names of tables matching a LIKE pattern
.timeout MS            Try opening locked tables for MS milliseconds
.width NUM NUM ...     Set column widths for "column" mode
sqlite&gt;

</literallayout> <!-- .fi -->

</refsect2>
</refsect1>

<refsect1 xml:id='options'><title>OPTIONS</title>
<para><command>sqlite3</command>
has the following options:</para>
<variablelist remap='TP'>
  <varlistentry>
  <term><option>-init file</option></term>
  <listitem>
<para>Read and execute commands from
<emphasis remap='I'>file</emphasis>
, which can contain a mix of SQL statements and meta-commands.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-echo</option></term>
  <listitem>
<para>Print commands before execution.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-[no]header</option></term>
  <listitem>
<para>Turn headers on or off.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-column</option></term>
  <listitem>
<para>Query results will be displayed in a table like form, using
whitespace characters to separate the columns and align the
output.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-html</option></term>
  <listitem>
<para>Query results will be output as simple HTML tables.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-line</option></term>
  <listitem>
<para>Query results will be displayed with one value per line, rows
separated by a blank line.  Designed to be easily parsed by
scripts or other programs</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-list</option></term>
  <listitem>
<para>Query results will be displayed with the separator (|, by default)
character between each field value.  The default.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-separator </option><replaceable>separator</replaceable></term>
  <listitem>
<para>Set output field separator.  Default is '|'.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-nullvalue </option><replaceable>string</replaceable></term>
  <listitem>
<para>Set string used to represent NULL values.  Default is ''
(empty string).</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-version</option></term>
  <listitem>
<para>Show SQLite version.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-help</option></term>
  <listitem>
<para>Show help on options and exit.</para>


  </listitem>
  </varlistentry>
</variablelist>
</refsect1>

<refsect1 xml:id='init_file'><title>INIT FILE</title>
<para><command>sqlite3</command>
reads an initialization file to set the configuration of the
interactive environment.  Throughout initialization, any previously
specified setting can be overridden.  The sequence of initialization is
as follows:</para>

<para>o The default configuration is established as follows:</para>


<literallayout remap='.nf'>
mode            = LIST
separator       = "|"
main prompt     = "sqlite&gt; "
continue prompt = "   ...&gt; "

</literallayout> <!-- .fi -->

<para>o If the file 
<filename>~/.sqliterc</filename>
exists, it is processed first.
can be found in the user's home directory, it is
read and processed.  It should generally only contain meta-commands.</para>

<para>o If the -init option is present, the specified file is processed.</para>

<para>o All other command line options are processed.</para>

</refsect1>

<refsect1 xml:id='see_also'><title>SEE ALSO</title>
<para>http://www.sqlite.org/
<!-- .br -->
The sqlite-doc package</para>
</refsect1>

<refsect1 xml:id='author'><title>AUTHOR</title>
<para>This manual page was originally written by Andreas Rottmann
&lt;rotty@debian.org&gt;, for the Debian GNU/Linux system (but may be used
by others).   It was subsequently revised by Bill Bumgarner &lt;bbum@mac.com&gt;.</para>
</refsect1>
</refentry>

