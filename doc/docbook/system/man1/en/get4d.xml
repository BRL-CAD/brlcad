<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='get4d1'>
<!--  Copyright (c) 1989, University of Utah -->
<refmeta>
<refentrytitle>GET4D</refentrytitle>
<manvolnum>1</manvolnum>
<refmiscinfo class='source'>June 20, 1989</refmiscinfo>
<refmiscinfo class='manual'>1</refmiscinfo>
</refmeta>
<refnamediv>
<refname>get4d</refname>
<refpurpose>get RLE images to a Silicon Graphics Iris/4D display</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<cmdsynopsis>
  <command>get4d</command>    
    <arg choice='opt'>-D </arg>
    <arg choice='opt'>-f </arg>
    <arg choice='opt'><arg choice='plain'>- </arg><arg choice='opt'><replaceable>GS</replaceable></arg></arg>
    <arg choice='opt'><arg choice='plain'>-g </arg><arg choice='plain'><replaceable>disp_gamma</replaceable></arg></arg>
    <arg choice='opt'><arg choice='plain'>- </arg><arg choice='opt'><replaceable>iI</replaceable></arg><arg choice='plain'><replaceable>image_gamma</replaceable></arg></arg>
    <arg choice='opt'>-n </arg>
    <arg choice='opt'><arg choice='plain'>-p </arg><arg choice='plain'><replaceable>xpos</replaceable></arg><arg choice='plain'><replaceable>ypos</replaceable></arg></arg>
    <arg choice='opt'><arg choice='plain'>-s </arg><arg choice='plain'><replaceable>xsize</replaceable></arg><arg choice='plain'><replaceable>ysize</replaceable></arg></arg>
    <arg choice='opt'>-w </arg>
    <arg choice='opt'><replaceable>infile</replaceable></arg>
    <sbr/>
</cmdsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para>This program displays an
<citerefentry><refentrytitle>RLE</refentrytitle><manvolnum>5</manvolnum></citerefentry>
file on a
<emphasis remap='I'>Silicon Graphics Iris/4D</emphasis>
display or
<emphasis remap='I'>IBM RS6000</emphasis>
with the GL library.</para>

<para>The default behavior is to display the image in RGB color.  An option is
provided to force black and white display.  There is currently no support in
<emphasis remap='I'>get4D</emphasis>
for non-24-bit color (lookup table modes), but the
<emphasis remap='I'>getmex (1)</emphasis>
program should work on 8-bit 4D's which cannot do RGB display.</para>

<para>The GT graphics fast pixel access routines are used by default on 4D/GT and
GTX machines, and Personal Irises.  The
<option>-G</option>
option is provided to force this mode, if the string returned by the
<citerefentry><refentrytitle>gversion</refentrytitle><manvolnum>3g</manvolnum></citerefentry>
function changes, or is different on future 4D's.</para>

<para>The penalty of GT mode is not being able to resize or pan the window, but
redisplay is so fast that there is no need to do so.  You can also go into
"slow mode" on GT machines by giving the
<option>-S</option>
flag.  Slow mode allows resizing the window and panning with the mouse.</para>
</refsect1>

<refsect1 xml:id='options'><title>OPTIONS</title>
<variablelist remap='TP'>
  <varlistentry>
  <term><option>-p </option><replaceable>xpos ypos</replaceable></term>
  <listitem>
<para>Position of the lower left corner of the window.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-s </option><replaceable>xsize ysize</replaceable></term>
  <listitem>
<para>Initial size of the window (slow mode only.)</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-f</option></term>
  <listitem>
<para>Normally,
<command>get4d</command>
will fork itself after putting the image on the screen, so that the
parent process may return the shell, leaving an "invisible" child to
keep the image refreshed.  If
<option>-f</option>
is specified, get4d will remain attached to the shell, whence it may be killed
with an interrupt signal.  In either case the window manager "quit" menu
button can be used to kill
<emphasis remap='I'>get4d.</emphasis></para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-g</option><replaceable> display_gamma</replaceable></term>
  <listitem>
<para>Specify the gamma of the display monitor.  If this flag is not specified,
<command>get4d</command>
looks in the user's home directory for a .gamma file.  This file is
produced by the
<citerefentry><refentrytitle>gamma</refentrytitle><manvolnum>1g</manvolnum></citerefentry>
SGI command (This is not done on the IBM R6000).  The value in the .gamma
file is used to determine the gamma of the display by calculating (2.4 /
<emphasis remap='I'>gamma_value</emphasis>
) and using that as the
<emphasis remap='I'>disp_gamma.</emphasis></para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-i</option><replaceable> image_gamma</replaceable></term>
  <listitem>
<para>Specify the gamma (contrast) of the image.  A low contrast image,
suited for direct display without compensation on a high contrast
monitor (as most monitors are) will have a gamma of less than one.
The default image gamma is 1.0.  Image gamma may also be specified by
a picture comment in the
<emphasis remap='I'>RLE (5)</emphasis>
file of the form
<emphasis remap='B'>image_gamma=</emphasis><emphasis remap='I'>gamma.</emphasis>
The command line argument will override the value in the file if specified.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-I</option><replaceable> image_gamma</replaceable></term>
  <listitem>
<para>An alternate method of specifying the image gamma, the number
following
<option>-I</option>
is the gamma of the display for which the image was originally
computed (and is therefore 1.0 divided by the actual gamma of the
image).  Image display gamma may also be specified by
a picture comment in the
<emphasis remap='I'>RLE (5)</emphasis>
file of the form
<emphasis remap='B'>display_gamma=</emphasis><emphasis remap='I'>gamma.</emphasis>
The command line argument will override the value in the file if specified.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-n</option></term>
  <listitem>
<para>Do not draw a window border.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-w</option></term>
  <listitem>
<para>This flag forces
<command>get4d</command>
to produce a gray scale dithered image instead of a color image.
Color input will be transformed to black and white via the
<emphasis remap='I'>NTSC Y</emphasis>
transform.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><option>-D</option></term>
  <listitem>
<para>"Debug mode".  The operations in the input
<citerefentry><refentrytitle>RLE</refentrytitle><manvolnum>5</manvolnum></citerefentry>
file will be printed as they are read.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='I'>file</emphasis></term>
  <listitem>
<para>Name of the
<citerefentry><refentrytitle>RLE</refentrytitle><manvolnum>5</manvolnum></citerefentry>
file to display.  If not specified, the image will be read from the
standard input.</para>
  </listitem>
  </varlistentry>
</variablelist>

<para>In "slow mode" You can "pan" a small window around in an image by
clicking the
<emphasis remap='I'>left mouse button</emphasis>
in the image.  The position in the image
under the cursor will jump to the center of the window.  The
<emphasis remap='I'>F9 key</emphasis>
or
<emphasis remap='I'>Alt keys</emphasis>
reset the view to position the center of the image in the center of the
window.  Furthermore,
<emphasis remap='I'>control-F9 (or control-Alt)</emphasis>
saves the current view, and
<emphasis remap='I'>shift-F9 (or shift-Alt)</emphasis>
restores it.</para>
</refsect1>

<refsect1 xml:id='note'><title>NOTE</title>
<para>If you have a shaded image that looks "too dark", it is probably because the
gamma is not set on the display.  (The default gamma is 1, which assumes that
gamma compensation will be done once and for all by programs producing images.)
<emphasis remap='I'>gamma 2</emphasis>
is better when the image producing program does not do the gamma correction.
You may want to put a gamma command in your .login file.</para>
</refsect1>

<refsect1 xml:id='see_also'><title>SEE ALSO</title>
<para><citerefentry><refentrytitle>getmex</refentrytitle><manvolnum>1</manvolnum></citerefentry>,
<citerefentry><refentrytitle>urt</refentrytitle><manvolnum>1</manvolnum></citerefentry>,
<citerefentry><refentrytitle>gversion</refentrytitle><manvolnum>3g</manvolnum></citerefentry>,
<citerefentry><refentrytitle>gamma</refentrytitle><manvolnum>1g</manvolnum></citerefentry>,
<citerefentry><refentrytitle>RLE</refentrytitle><manvolnum>5</manvolnum></citerefentry>.</para>
</refsect1>

<refsect1 xml:id='author'><title>AUTHOR</title>
<para>Russ Fish, University of Utah.
Based on getX, by Spencer W. Thomas.</para>
</refsect1>
</refentry>

