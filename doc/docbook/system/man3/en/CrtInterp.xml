<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man+tkman by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='crtinterp3'>

<!-- '\" Copyright (c) 1989\-1993 The Regents of the University of California.
'\" Copyright (c) 1994\-1996 Sun Microsystems, Inc. -->

<!-- '\" See the file "license.terms" for information on usage and redistribution
'\" of this file, and for a DISCLAIMER OF ALL WARRANTIES.
'\" 
'\" RCS: @(#) $Id$
'\"  -->
<refentryinfo><date>7.5</date></refentryinfo>
<refmeta>
<refentrytitle>Tcl_CreateInterp</refentrytitle>
<manvolnum>3</manvolnum>
<refmiscinfo class='date'>7.5</refmiscinfo>
<refmiscinfo class='source'>Tcl</refmiscinfo>
<refmiscinfo class='manual'>Tcl Library Procedures</refmiscinfo>
</refmeta>
<refnamediv>
<refname>Tcl_CreateInterp</refname>
<refname>Tcl_DeleteInterp</refname>
<refname>Tcl_InterpDeleted</refname>
<refpurpose>create and delete Tcl command interpreters</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<funcsynopsis>
<funcsynopsisinfo>
#include &lt;tcl.h&gt;

</funcsynopsisinfo>
<funcprototype>
<funcdef><function>Tcl_Interp</function> *Tcl_CreateInterp</funcdef>
<void/></funcprototype>
<funcprototype>
<funcdef><function>Tcl_DeleteInterp</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_InterpDeleted</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='arguments'><title>ARGUMENTS</title>
<!-- .AS Tcl_Interp *interp -->
<informaltable>
<tgroup cols='3'>
<tbody>

<row><entry>"Tcl_Interp"</entry><entry>"*interp"</entry><entry>"in"</entry>
<entry>
<para>Token for interpreter to be destroyed.</para>
<!-- .BE -->
</entry>
</row>
</tbody>
</tgroup>
</informaltable>

</refsect1>

<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para><emphasis remap='B'>Tcl_CreateInterp</emphasis> creates a new interpreter structure and returns
a token for it.  The token is required in calls to most other Tcl
procedures, such as <emphasis remap='B'>Tcl_CreateCommand</emphasis>, <emphasis remap='B'>Tcl_Eval</emphasis>, and
<function>Tcl_DeleteInterp</function>.
Clients are only allowed to access a few of the fields of
Tcl_Interp structures;  see the <function>Tcl_Interp</function>
and <emphasis remap='B'>Tcl_CreateCommand</emphasis> man pages for details.
The new interpreter is initialized with the built-in Tcl commands
and with the variables documented in tclvars(n).  To bind in
additional commands, call <emphasis remap='B'>Tcl_CreateCommand</emphasis>.</para>

<para><function>Tcl_DeleteInterp</function> marks an interpreter as deleted; the interpreter
will eventually be deleted when all calls to <emphasis remap='B'>Tcl_Preserve</emphasis> for it have
been matched by calls to <emphasis remap='B'>Tcl_Release</emphasis>. At that time, all of the
resources associated with it, including variables, procedures, and
application-specific command bindings, will be deleted.  After
<function>Tcl_DeleteInterp</function> returns any attempt to use <emphasis remap='B'>Tcl_Eval</emphasis> on the
interpreter will fail and return <emphasis remap='B'>TCL_ERROR</emphasis>. After the call to
<function>Tcl_DeleteInterp</function> it is safe to examine the interpreter's result,
query or set the values of variables, define, undefine or retrieve
procedures, and examine the runtime evaluation stack. See below, in the
section <emphasis remap='B'>INTERPRETERS AND MEMORY MANAGEMENT</emphasis> for details.</para>

<para><function>Tcl_InterpDeleted</function> returns nonzero if <function>Tcl_DeleteInterp</function> was
called with <emphasis remap='I'>interp</emphasis> as its argument; this indicates that the
interpreter will eventually be deleted, when the last call to
<emphasis remap='B'>Tcl_Preserve</emphasis> for it is matched by a call to <emphasis remap='B'>Tcl_Release</emphasis>. If
nonzero is returned, further calls to <emphasis remap='B'>Tcl_Eval</emphasis> in this interpreter
will return <emphasis remap='B'>TCL_ERROR</emphasis>.</para>

<para><function>Tcl_InterpDeleted</function> is useful in deletion callbacks to distinguish
between when only the memory the callback is responsible for is being
deleted and when the whole interpreter is being deleted. In the former case
the callback may recreate the data being deleted, but this would lead to an
infinite loop if the interpreter were being deleted.</para>

</refsect1>

<refsect1 xml:id='interpreters_and_memory_management'><title>INTERPRETERS AND MEMORY MANAGEMENT</title>
<para><function>Tcl_DeleteInterp</function> can be called at any time on an interpreter that may
be used by nested evaluations and C code in various extensions. Tcl
implements a simple mechanism that allows callers to use interpreters
without worrying about the interpreter being deleted in a nested call, and
without requiring special code to protect the interpreter, in most cases.
This mechanism ensures that nested uses of an interpreter can safely
continue using it even after <function>Tcl_DeleteInterp</function> is called.</para>

<para>The mechanism relies on matching up calls to <emphasis remap='B'>Tcl_Preserve</emphasis> with calls
to <emphasis remap='B'>Tcl_Release</emphasis>. If <function>Tcl_DeleteInterp</function> has been called, only when
the last call to <emphasis remap='B'>Tcl_Preserve</emphasis> is matched by a call to
<emphasis remap='B'>Tcl_Release</emphasis>, will the interpreter be freed. See the manual entry for
<emphasis remap='B'>Tcl_Preserve</emphasis> for a description of these functions.</para>

<para>The rules for when the user of an interpreter must call <emphasis remap='B'>Tcl_Preserve</emphasis>
and <emphasis remap='B'>Tcl_Release</emphasis> are simple:</para>
<variablelist remap='TP'>
  <varlistentry>
  <term>Interpreters Passed As Arguments</term>
  <listitem>
<para>Functions that are passed an interpreter as an argument can safely use the
interpreter without any special protection. Thus, when you write an
extension consisting of new Tcl commands, no special code is needed to
protect interpreters received as arguments. This covers the majority of all
uses.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Interpreter Creation And Deletion</term>
  <listitem>
<para>When a new interpreter is created and used in a call to <emphasis remap='B'>Tcl_Eval</emphasis>,
<emphasis remap='B'>Tcl_VarEval</emphasis>, <emphasis remap='B'>Tcl_GlobalEval</emphasis>, <emphasis remap='B'>Tcl_SetVar</emphasis>, or
<emphasis remap='B'>Tcl_GetVar</emphasis>, a pair of calls to <emphasis remap='B'>Tcl_Preserve</emphasis> and
<emphasis remap='B'>Tcl_Release</emphasis> should be wrapped around all uses of the interpreter.
Remember that it is unsafe to use the interpreter once <emphasis remap='B'>Tcl_Release</emphasis>
has been called. To ensure that the interpreter is properly deleted when
it is no longer needed, call <function>Tcl_InterpDeleted</function> to test if some other
code already called <function>Tcl_DeleteInterp</function>; if not, call
<function>Tcl_DeleteInterp</function> before calling <emphasis remap='B'>Tcl_Release</emphasis> in your own code.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term>Retrieving An Interpreter From A Data Structure</term>
  <listitem>
<para>When an interpreter is retrieved from a data structure (e.g. the client
data of a callback) for use in <emphasis remap='B'>Tcl_Eval</emphasis>, <emphasis remap='B'>Tcl_VarEval</emphasis>,
<emphasis remap='B'>Tcl_GlobalEval</emphasis>, <emphasis remap='B'>Tcl_SetVar</emphasis>, or <emphasis remap='B'>Tcl_GetVar</emphasis>, a pair of
calls to <emphasis remap='B'>Tcl_Preserve</emphasis> and <emphasis remap='B'>Tcl_Release</emphasis> should be wrapped around
all uses of the interpreter; it is unsafe to reuse the interpreter once
<emphasis remap='B'>Tcl_Release</emphasis> has been called.  If an interpreter is stored inside a
callback data structure, an appropriate deletion cleanup mechanism should
be set up by the code that creates the data structure so that the
interpreter is removed from the data structure (e.g. by setting the field
to NULL) when the interpreter is deleted. Otherwise, you may be using an
interpreter that has been freed and whose memory may already have been
reused.</para>
  </listitem>
  </varlistentry>
</variablelist>

<para>All uses of interpreters in Tcl and Tk have already been protected.
Extension writers should ensure that their code also properly protects any
additional interpreters used, as described above.</para>

</refsect1>

<refsect1 xml:id='see_also'><title>SEE ALSO</title>
<para>Tcl_P<citerefentry><refentrytitle>reserve</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_R<citerefentry><refentrytitle>elease</refentrytitle><manvolnum>3</manvolnum></citerefentry></para>

</refsect1>

<refsect1 xml:id='keywords'><title>KEYWORDS</title>
<para>command, create, delete, interpreter</para>
</refsect1>
</refentry>

