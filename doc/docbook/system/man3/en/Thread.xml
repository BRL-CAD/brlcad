<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man+tkman by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='thread3'>

<!-- '\" Copyright (c) 1999 Scriptics Corporation
'\" Copyright (c) 1998 Sun Microsystems, Inc. -->

<!-- '\" See the file "license.terms" for information on usage and redistribution
'\" of this file, and for a DISCLAIMER OF ALL WARRANTIES.
'\" 
'\" RCS: @(#) $Id$
'\"  -->
<refentryinfo><date>8.1</date></refentryinfo>
<refmeta>
<refentrytitle>Threads</refentrytitle>
<manvolnum>3</manvolnum>
<refmiscinfo class='date'>8.1</refmiscinfo>
<refmiscinfo class='source'>Tcl</refmiscinfo>
<refmiscinfo class='manual'>Tcl Library Procedures</refmiscinfo>
</refmeta>
<refnamediv>
<refname>Tcl_ConditionNotify</refname>
<refname>Tcl_ConditionWait</refname>
<refname>Tcl_ConditionFinalize</refname>
<refname>Tcl_GetThreadData</refname>
<refname>Tcl_MutexLock</refname>
<refname>Tcl_MutexUnlock</refname>
<refname>Tcl_MutexFinalize</refname>
<refname>Tcl_CreateThread</refname>
<refname>Tcl_JoinThread</refname>
<refpurpose>Tcl thread support</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<funcsynopsis>
<funcsynopsisinfo>
#include &lt;tcl.h&gt;

</funcsynopsisinfo>
<funcprototype>
<funcdef>void <function>Tcl_ConditionNotify</function></funcdef>
<paramdef><parameter>condPtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tcl_ConditionWait</function></funcdef>
<paramdef><parameter>condPtr</parameter></paramdef>
<paramdef><parameter>mutexPtr</parameter></paramdef>
<paramdef><parameter>timePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tcl_ConditionFinalize</function></funcdef>
<paramdef><parameter>condPtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Void</function> *Tcl_GetThreadData</funcdef>
<paramdef><parameter>keyPtr</parameter></paramdef>
<paramdef><parameter>size</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tcl_MutexLock</function></funcdef>
<paramdef><parameter>mutexPtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tcl_MutexUnlock</function></funcdef>
<paramdef><parameter>mutexPtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tcl_MutexFinalize</function></funcdef>
<paramdef><parameter>mutexPtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_CreateThread</function></funcdef>
<paramdef><parameter>idPtr</parameter></paramdef>
<paramdef><parameter>threadProc</parameter></paramdef>
<paramdef><parameter>clientData</parameter></paramdef>
<paramdef><parameter>stackSize</parameter></paramdef>
<paramdef><parameter>flags</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_JoinThread</function></funcdef>
<paramdef><parameter>id</parameter></paramdef>
<paramdef><parameter>result</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='arguments'><title>ARGUMENTS</title>
<!-- .AS Tcl_CreateThreadProc threadProc out -->
<informaltable>
<tgroup cols='3'>
<tbody>

<row><entry>"Tcl_Condition"</entry><entry>"*condPtr"</entry><entry>"in"</entry>
<entry>
<para>A condition variable, which must be associated with a mutex lock.</para>
</entry>
</row>
<row><entry>Tcl_Mutex</entry><entry>*mutexPtr</entry><entry>in</entry>
<entry>
<para>A mutex lock.</para>
</entry>
</row>
<row><entry>Tcl_Time</entry><entry>*timePtr</entry><entry>in</entry>
<entry>
<para>A time limit on the condition wait.  NULL to wait forever.
Note that a polling value of 0 seconds does not make much sense.</para>
</entry>
</row>
<row><entry>Tcl_ThreadDataKey</entry><entry>*keyPtr</entry><entry>in</entry>
<entry>
<para>This identifies a block of thread local storage.  The key should be
static and process-wide, yet each thread will end up associating
a different block of storage with this key.</para>
</entry>
</row>
<row><entry>int</entry><entry>*size</entry><entry>in</entry>
<entry>
<para>The size of the thread local storage block.  This amount of data
is allocated and initialized to zero the first time each thread
calls <emphasis remap='B'>Tcl_GetThreadData</emphasis>.</para>
</entry>
</row>
<row><entry>Tcl_ThreadId</entry><entry>*idPtr</entry><entry>out</entry>
<entry>
<para>The referred storage will contain the id of the newly created thread as
returned by the operating system.</para>
</entry>
</row>
<row><entry>Tcl_ThreadId</entry><entry>id</entry><entry>in</entry>
<entry>
<para>Id of the thread waited upon.</para>
</entry>
</row>
<row><entry>Tcl_ThreadCreateProc</entry><entry>threadProc</entry><entry>in</entry>
<entry>
<para>This procedure will act as the <function>main()</function> of the newly created
thread. The specified <emphasis remap='I'>clientData</emphasis> will be its sole argument.</para>
</entry>
</row>
<row><entry>ClientData</entry><entry>clientData</entry><entry>in</entry>
<entry>
<para>Arbitrary information. Passed as sole argument to the <emphasis remap='I'>threadProc</emphasis>.</para>
</entry>
</row>
<row><entry>int</entry><entry>stackSize</entry><entry>in</entry>
<entry>
<para>The size of the stack given to the new thread.</para>
</entry>
</row>
<row><entry>int</entry><entry>flags</entry><entry>in</entry>
<entry>
<para>Bitmask containing flags allowing the caller to modify behaviour of
the new thread.</para>
</entry>
</row>
<row><entry>int</entry><entry>*result</entry><entry>out</entry>
<entry>
<para>The referred storage is used to place the exit code of the thread
waited upon into it.</para>
<!-- .BE -->
</entry>
</row>
</tbody>
</tgroup>
</informaltable>

</refsect1>

<refsect1 xml:id='introduction'><title>INTRODUCTION</title>
<para>Beginning with the 8.1 release, the Tcl core is thread safe, which
allows you to incorporate Tcl into multithreaded applications without
customizing the Tcl core.  To enable Tcl multithreading support,
you must include the <option>--enable-threads</option> option to <emphasis remap='B'>configure</emphasis>
when you configure and compile your Tcl core.</para>

<para>An important constraint of the Tcl threads implementation is that
<emphasis remap='I'>only the thread that created a Tcl interpreter can use that
interpreter</emphasis>.  In other words, multiple threads can not access
the same Tcl interpreter.  (However, a single thread can safely create
and use multiple interpreters.)</para>
</refsect1>

<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para>Tcl provides <function>Tcl_CreateThread</function> for creating threads. The
caller can determine the size of the stack given to the new thread and
modify the behaviour through the supplied <emphasis remap='I'>flags</emphasis>. The value
<emphasis remap='B'>TCL_THREAD_STACK_DEFAULT</emphasis> for the <emphasis remap='I'>stackSize</emphasis> indicates that
the default size as specified by the operating system is to be used
for the new thread. As for the flags, currently only the values
<emphasis remap='B'>TCL_THREAD_NOFLAGS</emphasis> and <emphasis remap='B'>TCL_THREAD_JOINABLE</emphasis> are defined. The
first of them invokes the default behaviour with no
specialties. Using the second value marks the new thread as
<emphasis remap='I'>joinable</emphasis>. This means that another thread can wait for the such
marked thread to exit and join it.</para>

<para>Restrictions: On some UNIX systems the pthread-library does not
contain the functionality to specify the stack size of a thread. The
specified value for the stack size is ignored on these systems.
Windows currently does not support joinable threads. This
flag value is therefore ignored on this platform.</para>

<para>Tcl provides the <emphasis remap='B'>Tcl_ExitThread</emphasis> and <emphasis remap='B'>Tcl_FinalizeThread</emphasis> functions
for terminating threads and invoking optional per-thread exit
handlers.  See the <emphasis remap='B'>Tcl_Exit</emphasis> page for more information on these
procedures.</para>

<para>The <function>Tcl_JoinThread</function> function is provided to allow threads to wait
upon the exit of another thread, which must have been marked as
joinable through usage of the <emphasis remap='B'>TCL_THREAD_JOINABLE</emphasis>-flag during
its creation via <function>Tcl_CreateThread</function>.</para>

<para>Trying to wait for the exit of a non-joinable thread or a thread which
is already waited upon will result in an error. Waiting for a joinable
thread which already exited is possible, the system will retain the
necessary information until after the call to <function>Tcl_JoinThread</function>.
This means that not calling <function>Tcl_JoinThread</function> for a joinable thread
will cause a memory leak.</para>

<para>The <emphasis remap='B'>Tcl_GetThreadData</emphasis> call returns a pointer to a block of
thread-private data.  Its argument is a key that is shared by all threads
and a size for the block of storage.  The storage is automatically 
allocated and initialized to all zeros the first time each thread asks for it.
The storage is automatically deallocated by <emphasis remap='B'>Tcl_FinalizeThread</emphasis>.</para>

<refsect2 xml:id='synchronization_and_communication'><title>SYNCHRONIZATION AND COMMUNICATION</title>
<para>Tcl provides <emphasis remap='B'>Tcl_ThreadQueueEvent</emphasis> and <emphasis remap='B'>Tcl_ThreadAlert</emphasis>
for handling event queuing in multithreaded applications.  See
the <emphasis remap='B'>Notifier</emphasis> manual page for more information on these procedures.</para>

<para>A mutex is a lock that is used to serialize all threads through a piece
of code by calling <function>Tcl_MutexLock</function> and <function>Tcl_MutexUnlock</function>.
If one thread holds a mutex, any other thread calling <function>Tcl_MutexLock</function> will
block until <function>Tcl_MutexUnlock</function> is called.
A mutex can be destroyed after its use by calling <function>Tcl_MutexFinalize</function>.
The result of locking a mutex twice from the same thread is undefined.
On some platforms it will result in a deadlock.
The <function>Tcl_MutexLock</function>, <function>Tcl_MutexUnlock</function> and <function>Tcl_MutexFinalize</function>
procedures are defined as empty macros if not compiling with threads enabled.
For declaration of mutexes the <emphasis remap='B'>TCL_DECLARE_MUTEX</emphasis> macro should be used.
This macro assures correct mutex handling even when the core is compiled
without threads enabled.</para>

<para>A condition variable is used as a signaling mechanism:
a thread can lock a mutex and then wait on a condition variable
with <function>Tcl_ConditionWait</function>.  This atomically releases the mutex lock
and blocks the waiting thread until another thread calls
<function>Tcl_ConditionNotify</function>.  The caller of <function>Tcl_ConditionNotify</function> should
have the associated mutex held by previously calling <function>Tcl_MutexLock</function>,
but this is not enforced.  Notifying the
condition variable unblocks all threads waiting on the condition variable,
but they do not proceed until the mutex is released with <function>Tcl_MutexUnlock</function>.
The implementation of <function>Tcl_ConditionWait</function> automatically locks
the mutex before returning.</para>

<para>The caller of <function>Tcl_ConditionWait</function> should be prepared for spurious
notifications by calling <function>Tcl_ConditionWait</function> within a while loop
that tests some invariant.</para>

<para>A condition variable can be destroyed after its use by calling
<function>Tcl_ConditionFinalize</function>.</para>

<para>The <function>Tcl_ConditionNotify</function>, <function>Tcl_ConditionWait</function> and
<function>Tcl_ConditionFinalize</function> procedures are defined as empty macros if
not compiling with threads enabled.</para>
</refsect2>

<refsect2 xml:id='initialization'><title>INITIALIZATION</title>

<para>All of these synchronization objects are self-initializing.
They are implemented as opaque pointers that should be NULL
upon first use.
The mutexes and condition variables are
either cleaned up by process exit handlers (if living that long) or
explicitly by calls to <function>Tcl_MutexFinalize</function> or
<function>Tcl_ConditionFinalize</function>.
Thread local storage is reclaimed during <emphasis remap='B'>Tcl_FinalizeThread</emphasis>.</para>
</refsect2>
</refsect1>

<refsect1 xml:id='scriptlevel_access_to_threads'><title>SCRIPT-LEVEL ACCESS TO THREADS</title>
<!-- .VS 8.5 -->
<para>Tcl provides no built-in commands for scripts to use to create,
manage, or join threads, nor any script-level access to mutex or
condition variables.  It provides such facilities only via C
interfaces, and leaves it up to packages to expose these matters to
the script level.  One such package is the <emphasis remap='B'>Thread</emphasis> package.</para>
<!-- .VE 8.5 -->
</refsect1>

<refsect1 xml:id='see_also'><title>SEE ALSO</title>
<para>Tcl_GetCurrentT<citerefentry><refentrytitle>hread</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_ThreadQueueE<citerefentry><refentrytitle>vent</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_ThreadA<citerefentry><refentrytitle>lert</refentrytitle><manvolnum>3</manvolnum></citerefentry>,
Tcl_ExitT<citerefentry><refentrytitle>hread</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_FinalizeT<citerefentry><refentrytitle>hread</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_CreateThreadExitH<citerefentry><refentrytitle>andler</refentrytitle><manvolnum>3</manvolnum></citerefentry>,
Tcl_DeleteThreadExitH<citerefentry><refentrytitle>andler</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Thread</para>
</refsect1>

<refsect1 xml:id='keywords'><title>KEYWORDS</title>
<para>thread, mutex, condition variable, thread local storage</para>
</refsect1>
</refentry>

