<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man+tkman by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='dooneevent3'>

<!-- '\" Copyright (c) 1990\-1992 The Regents of the University of California.
'\" Copyright (c) 1994\-1996 Sun Microsystems, Inc. -->

<!-- '\" See the file "license.terms" for information on usage and redistribution
'\" of this file, and for a DISCLAIMER OF ALL WARRANTIES.
'\" 
'\" RCS: @(#) $Id$
'\"  -->
<refentryinfo><date>7.5</date></refentryinfo>
<refmeta>
<refentrytitle>Tcl_DoOneEvent</refentrytitle>
<manvolnum>3</manvolnum>
<refmiscinfo class='date'>7.5</refmiscinfo>
<refmiscinfo class='source'>Tcl</refmiscinfo>
<refmiscinfo class='manual'>Tcl Library Procedures</refmiscinfo>
</refmeta>
<refnamediv>
<refname>Tcl_DoOneEvent</refname>
<refpurpose>wait for events and invoke event handlers</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<funcsynopsis>
<funcsynopsisinfo>
#include &lt;tcl.h&gt;

</funcsynopsisinfo>
<funcprototype>
<funcdef>int <function>Tcl_DoOneEvent</function></funcdef>
<paramdef><parameter>flags</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='arguments'><title>ARGUMENTS</title>
<!-- .AS int flags -->
<informaltable>
<tgroup cols='3'>
<tbody>

<row><entry>"int"</entry><entry>"flags"</entry><entry>"in"</entry>
<entry>
<para>This parameter is normally zero.  It may be an OR-ed combination
of any of the following flag bits:  
<emphasis remap='B'>TCL_WINDOW_EVENTS</emphasis>, <emphasis remap='B'>TCL_FILE_EVENTS</emphasis>,
<emphasis remap='B'>TCL_TIMER_EVENTS</emphasis>, <emphasis remap='B'>TCL_IDLE_EVENTS</emphasis>, <emphasis remap='B'>TCL_ALL_EVENTS</emphasis>,
or <emphasis remap='B'>TCL_DONT_WAIT</emphasis>.</para>
<!-- .BE -->
</entry>
</row>
</tbody>
</tgroup>
</informaltable>

</refsect1>

<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para>This procedure is the entry point to Tcl's event loop; it is responsible for
waiting for events and dispatching event handlers created with
procedures such as <emphasis remap='B'>Tk_CreateEventHandler</emphasis>, <emphasis remap='B'>Tcl_CreateFileHandler</emphasis>,
<emphasis remap='B'>Tcl_CreateTimerHandler</emphasis>, and <emphasis remap='B'>Tcl_DoWhenIdle</emphasis>.
<function>Tcl_DoOneEvent</function> checks to see if
events are already present on the Tcl event queue; if so,
it calls the handler(s) for the first (oldest) event, removes it from
the queue, and returns.
If there are no events ready to be handled, then <function>Tcl_DoOneEvent</function>
checks for new events from all possible sources.
If any are found, it puts all of them on Tcl's event queue, calls
handlers for the first event on the queue, and returns.
If no events are found, <function>Tcl_DoOneEvent</function> checks for <emphasis remap='B'>Tcl_DoWhenIdle</emphasis>
callbacks; if any are found, it invokes all of them and returns.
Finally, if no events or idle callbacks have been found, then
<function>Tcl_DoOneEvent</function> sleeps until an event occurs; then it adds any 
new events to the Tcl event queue, calls handlers for the first event,
and returns.
The normal return value is 1 to signify that some event
was processed (see below for other alternatives).</para>

<para>If the <emphasis remap='I'>flags</emphasis> argument to <function>Tcl_DoOneEvent</function> is non-zero,
it restricts the kinds of events that will be processed by
<function>Tcl_DoOneEvent</function>.
<emphasis remap='I'>Flags</emphasis> may be an OR-ed combination of any of the following bits:</para>
<variablelist remap='TP'>
  <varlistentry>
  <term><emphasis remap='B'>TCL_WINDOW_EVENTS</emphasis> -</term>
  <listitem>
<para>Process window system events.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='B'>TCL_FILE_EVENTS</emphasis> -</term>
  <listitem>
<para>Process file events.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='B'>TCL_TIMER_EVENTS</emphasis> -</term>
  <listitem>
<para>Process timer events.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='B'>TCL_IDLE_EVENTS</emphasis> -</term>
  <listitem>
<para>Process idle callbacks.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='B'>TCL_ALL_EVENTS</emphasis> -</term>
  <listitem>
<para>Process all kinds of events:  equivalent to OR-ing together all of the
above flags or specifying none of them.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='B'>TCL_DONT_WAIT</emphasis> -</term>
  <listitem>
<para>Do not sleep:  process only events that are ready at the time of the
call.</para>
  </listitem>
  </varlistentry>
</variablelist>

<para>If any of the flags <emphasis remap='B'>TCL_WINDOW_EVENTS</emphasis>, <emphasis remap='B'>TCL_FILE_EVENTS</emphasis>,
<emphasis remap='B'>TCL_TIMER_EVENTS</emphasis>, or <emphasis remap='B'>TCL_IDLE_EVENTS</emphasis> is set, then the only
events that will be considered are those for which flags are set.
Setting none of these flags is equivalent to the value
<emphasis remap='B'>TCL_ALL_EVENTS</emphasis>, which causes all event types to be processed.
If an application has defined additional event sources with
<emphasis remap='B'>Tcl_CreateEventSource</emphasis>, then additional <emphasis remap='I'>flag</emphasis> values
may also be valid, depending on those event sources.</para>

<para>The <emphasis remap='B'>TCL_DONT_WAIT</emphasis> flag causes <function>Tcl_DoOneEvent</function> not to put
the process to sleep:  it will check for events but if none are found
then it returns immediately with a return value of 0 to indicate
that no work was done.
<function>Tcl_DoOneEvent</function> will also return 0 without doing anything if
the only alternative is to block forever (this can happen, for example,
if <emphasis remap='I'>flags</emphasis> is <emphasis remap='B'>TCL_IDLE_EVENTS</emphasis> and there are no
<emphasis remap='B'>Tcl_DoWhenIdle</emphasis> callbacks pending, or if no event handlers or
timer handlers exist).</para>

<para><function>Tcl_DoOneEvent</function> may be invoked recursively.  For example,
it is possible to invoke <function>Tcl_DoOneEvent</function> recursively
from a handler called by <function>Tcl_DoOneEvent</function>.  This sort
of operation is useful in some modal situations, such
as when a
notification dialog has been popped up and an application wishes to
wait for the user to click a button in the dialog before
doing anything else.</para>

</refsect1>

<refsect1 xml:id='keywords'><title>KEYWORDS</title>
<para>callback, event, handler, idle, timer</para>
</refsect1>
</refentry>

