<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man+tkman by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='findphoto3'>

<!-- '\" Copyright (c) 1994 The Australian National University
'\" Copyright (c) 1994\-1996 Sun Microsystems, Inc. -->

<!-- '\" See the file "license.terms" for information on usage and redistribution
'\" of this file, and for a DISCLAIMER OF ALL WARRANTIES.
'\" 
'\" Author: Paul Mackerras (paulus@cs.anu.edu.au),
'\"	    Department of Computer Science,
'\"	    Australian National University. -->

<!-- '\" RCS: @(#) $Id$ -->

<refentryinfo><date>8.0</date></refentryinfo>
<refmeta>
<refentrytitle>Tk_FindPhoto</refentrytitle>
<manvolnum>3</manvolnum>
<refmiscinfo class='date'>8.0</refmiscinfo>
<refmiscinfo class='source'>Tk</refmiscinfo>
<refmiscinfo class='manual'>Tk Library Procedures</refmiscinfo>
</refmeta>
<refnamediv>
<refname>Tk_FindPhoto</refname>
<refname>Tk_PhotoPutBlock</refname>
<refname>Tk_PhotoPutZoomedBlock</refname>
<refname>Tk_PhotoGetImage</refname>
<refname>Tk_PhotoBlank</refname>
<refname>Tk_PhotoExpand</refname>
<refname>Tk_PhotoGetSize</refname>
<refname>Tk_PhotoSetSize</refname>
<refpurpose>manipulate the image data stored in a photo image.</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<funcsynopsis>
<funcsynopsisinfo>
#include &lt;tk.h&gt;

</funcsynopsisinfo>
<funcprototype>
<funcdef>Tk_PhotoHandle <function>Tk_FindPhoto</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
<paramdef><parameter>imageName</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tk_PhotoPutBlock</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
<paramdef><parameter>handle</parameter></paramdef>
<paramdef><parameter>blockPtr</parameter></paramdef>
<paramdef><parameter>x</parameter></paramdef>
<paramdef><parameter>y</parameter></paramdef>
<paramdef><parameter>width</parameter></paramdef>
<paramdef><parameter>height</parameter></paramdef>
<paramdef><parameter>compRule</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tk_PhotoPutZoomedBlock</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
<paramdef><parameter>handle</parameter></paramdef>
<paramdef><parameter>blockPtr</parameter></paramdef>
<paramdef><parameter>x</parameter></paramdef>
<paramdef><parameter>y</parameter></paramdef>
<paramdef><parameter>width</parameter></paramdef>
<paramdef><parameter>height</parameter></paramdef>
<paramdef><parameter>zoomX</parameter></paramdef>
<paramdef><parameter>zoomY</parameter></paramdef>
<paramdef><parameter>subsampleX</parameter></paramdef>
<paramdef><parameter>subsampleY</parameter></paramdef>
<paramdef><parameter>compRule</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tk_PhotoGetImage</function></funcdef>
<paramdef><parameter>handle</parameter></paramdef>
<paramdef><parameter>blockPtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tk_PhotoBlank</function></funcdef>
<paramdef><parameter>handle</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tk_PhotoExpand</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
<paramdef><parameter>handle</parameter></paramdef>
<paramdef><parameter>width</parameter></paramdef>
<paramdef><parameter>height</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tk_PhotoGetSize</function></funcdef>
<paramdef><parameter>handle</parameter></paramdef>
<paramdef><parameter>widthPtr</parameter></paramdef>
<paramdef><parameter>heightPtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tk_PhotoSetSize</function></funcdef>
    <paramdef>interp. <parameter>handle</parameter></paramdef>
    <paramdef><parameter>width</parameter></paramdef>
    <paramdef><parameter>height</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='arguments'><title>ARGUMENTS</title>
<!-- .AS Tk_PhotoImageBlock window_path -->
<informaltable>
<tgroup cols='3'>
<tbody>

<row><entry>"Tcl_Interp"</entry><entry>"*interp"</entry><entry>"in"</entry>
<entry>
<para>Interpreter in which image was created and in which error reporting is
to be done.</para>
</entry>
</row>
<row><entry>"const char"</entry><entry>*imageName</entry><entry>in</entry>
<entry>
<para>Name of the photo image.</para>
</entry>
</row>
<row><entry>Tk_PhotoHandle</entry><entry>handle</entry><entry>in</entry>
<entry>
<para>Opaque handle identifying the photo image to be affected.</para>
</entry>
</row>
<row><entry>Tk_PhotoImageBlock</entry><entry>*blockPtr</entry><entry>in</entry>
<entry>
<para>Specifies the address and storage layout of image data.</para>
</entry>
</row>
<row><entry>int</entry><entry>x</entry><entry>in</entry>
<entry>
<para>Specifies the X coordinate where the top-left corner of the block is
to be placed within the image.</para>
</entry>
</row>
<row><entry>int</entry><entry>y</entry><entry>in</entry>
<entry>
<para>Specifies the Y coordinate where the top-left corner of the block is
to be placed within the image.</para>
</entry>
</row>
<row><entry>int</entry><entry>width</entry><entry>in</entry>
<entry>
<para>Specifies the width of the image area to be affected (for
<function>Tk_PhotoPutBlock</function>) or the desired image width (for
<function>Tk_PhotoExpand</function> and <function>Tk_PhotoSetSize</function>).</para>
</entry>
</row>
<row><entry>int</entry><entry>compRule</entry><entry>in</entry>
<entry>
<para>Specifies the compositing rule used when combining transparent pixels
in a block of data with a photo image.  Must be one of
<emphasis remap='B'>TK_PHOTO_COMPOSITE_OVERLAY</emphasis> (which puts the block of data over the top
of the existing photo image, with the previous contents showing
through in the transparent bits) or <emphasis remap='B'>TK_PHOTO_COMPOSITE_SET</emphasis> (which
discards the existing photo image contents in the rectangle covered by
the data block.)</para>
</entry>
</row>
<row><entry>int</entry><entry>height</entry><entry>in</entry>
<entry>
<para>Specifies the height of the image area to be affected (for
<function>Tk_PhotoPutBlock</function>) or the desired image height (for
<function>Tk_PhotoExpand</function> and <function>Tk_PhotoSetSize</function>).</para>
</entry>
</row>
<row><entry>int</entry><entry>*widthPtr</entry><entry>out</entry>
<entry>
<para>Pointer to location in which to store the image width.</para>
</entry>
</row>
<row><entry>int</entry><entry>*heightPtr</entry><entry>out</entry>
<entry>
<para>Pointer to location in which to store the image height.</para>
</entry>
</row>
<row><entry>int</entry><entry>subsampleX</entry><entry>in</entry>
<entry>
<para>Specifies the subsampling factor in the X direction for input
image data.</para>
</entry>
</row>
<row><entry>int</entry><entry>subsampleY</entry><entry>in</entry>
<entry>
<para>Specifies the subsampling factor in the Y direction for input
image data.</para>
</entry>
</row>
<row><entry>int</entry><entry>zoomX</entry><entry>in</entry>
<entry>
<para>Specifies the zoom factor to be applied in the X direction to pixels
being written to the photo image.</para>
</entry>
</row>
<row><entry>int</entry><entry>zoomY</entry><entry>in</entry>
<entry>
<para>Specifies the zoom factor to be applied in the Y direction to pixels
being written to the photo image.</para>
<!-- .BE -->
</entry>
</row>
</tbody>
</tgroup>
</informaltable>

</refsect1>

<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para><function>Tk_FindPhoto</function> returns an opaque handle that is used to identify a
particular photo image to the other procedures.  The parameter is the
name of the image, that is, the name specified to the <emphasis remap='B'>image create
photo</emphasis> command, or assigned by that command if no name was specified.</para>

<para><function>Tk_PhotoPutBlock</function> is used to supply blocks of image data to be
displayed.  The call affects an area of the image of size
<emphasis remap='I'>width</emphasis> x <emphasis remap='I'>height</emphasis> pixels, with its top-left corner at
coordinates (<emphasis remap='I'>x</emphasis>,<emphasis remap='I'>y</emphasis>).  All of <emphasis remap='I'>width</emphasis>, <emphasis remap='I'>height</emphasis>,
<emphasis remap='I'>x</emphasis>, and <emphasis remap='I'>y</emphasis> must be non-negative.
If part of this area lies outside the
current bounds of the image, the image will be expanded to include the
area, unless the user has specified an explicit image size with the
<option>-width</option> and/or <option>-height</option> widget configuration options
(see photo(n)); in that
case the area is silently clipped to the image boundaries.</para>

<para>The <emphasis remap='I'>block</emphasis> parameter is a pointer to a
<type>Tk_PhotoImageBlock</type> structure, defined as follows:</para>
<programlisting remap='CS'>
typedef struct {
    unsigned char *<emphasis remap='I'>pixelPtr</emphasis>;
    int <emphasis remap='I'>width</emphasis>;
    int <emphasis remap='I'>height</emphasis>;
    int <emphasis remap='I'>pitch</emphasis>;
    int <emphasis remap='I'>pixelSize</emphasis>;
    int <emphasis remap='I'>offset[4]</emphasis>;
} Tk_PhotoImageBlock;
</programlisting> <!-- remap='CE' -->
<para>The <emphasis remap='I'>pixelPtr</emphasis> field points to the first pixel, that is, the
top-left pixel in the block.
The <emphasis remap='I'>width</emphasis> and <emphasis remap='I'>height</emphasis> fields specify the dimensions of the
block of pixels.  The <emphasis remap='I'>pixelSize</emphasis> field specifies the address
difference between two horizontally adjacent pixels.  Often it is 3
or 4, but it can have any value.  The <emphasis remap='I'>pitch</emphasis> field specifies the
address difference between two vertically adjacent pixels.  The
<emphasis remap='I'>offset</emphasis> array contains the offsets from the address of a pixel
to the addresses of the bytes containing the red, green, blue and alpha
(transparency) components.  These are normally 0, 1, 2 and 3, but can
have other values, e.g., for images that are stored as separate red,
green and blue planes.</para>

<para>The <emphasis remap='I'>compRule</emphasis> parameter to <function>Tk_PhotoPutBlock</function> specifies a
compositing rule that says what to do with transparent pixels.  The
value <emphasis remap='B'>TK_PHOTO_COMPOSITE_OVERLAY</emphasis> says that the previous contents of
the photo image should show through, and the value
<emphasis remap='B'>TK_PHOTO_COMPOSITE_SET</emphasis> says that the previous contents of the photo
image should be completely ignored, and the values from the block be
copied directly across.  The behavior in Tk8.3 and earlier was
equivalent to having <emphasis remap='B'>TK_PHOTO_COMPOSITE_OVERLAY</emphasis> as a compositing rule.</para>

<para>The value given for the <emphasis remap='I'>width</emphasis> and <emphasis remap='I'>height</emphasis> parameters to
<function>Tk_PhotoPutBlock</function> do not have to correspond to the values specified
in <emphasis remap='I'>block</emphasis>.  If they are smaller, <function>Tk_PhotoPutBlock</function> extracts a
sub-block from the image data supplied.  If they are larger, the data
given are replicated (in a tiled fashion) to fill the specified area.
These rules operate independently in the horizontal and vertical
directions.</para>

<!-- .VS 8.5 -->
<para><function>Tk_PhotoPutBlock</function> normally returns <emphasis remap='B'>TCL_OK</emphasis>, though if it cannot
allocate sufficient memory to hold the resulting image, <emphasis remap='B'>TCL_ERROR</emphasis> is
returned instead and, if the <emphasis remap='I'>interp</emphasis> argument is non-NULL, an
error message is placed in the interpreter's result.</para>
<!-- .VE 8.5 -->

<para><function>Tk_PhotoPutZoomedBlock</function> works like <function>Tk_PhotoPutBlock</function> except that
the image can be reduced or enlarged for display.  The
<emphasis remap='I'>subsampleX</emphasis> and <emphasis remap='I'>subsampleY</emphasis> parameters allow the size of the
image to be reduced by subsampling.
<function>Tk_PhotoPutZoomedBlock</function> will use only pixels from the input image
whose X coordinates are multiples of <emphasis remap='I'>subsampleX</emphasis>, and whose Y
coordinates are multiples of <emphasis remap='I'>subsampleY</emphasis>.  For example, an image
of 512x512 pixels can be reduced to 256x256 by setting
<emphasis remap='I'>subsampleX</emphasis> and <emphasis remap='I'>subsampleY</emphasis> to 2.</para>

<para>The <emphasis remap='I'>zoomX</emphasis> and <emphasis remap='I'>zoomY</emphasis> parameters allow the image to be
enlarged by pixel replication.  Each pixel of the (possibly subsampled)
input image will be written to a block <emphasis remap='I'>zoomX</emphasis> pixels wide and
<emphasis remap='I'>zoomY</emphasis> pixels high of the displayed image.  Subsampling and
zooming can be used together for special effects.</para>

<para><function>Tk_PhotoGetImage</function> can be used to retrieve image data from a photo
image.  <function>Tk_PhotoGetImage</function> fills
in the structure pointed to by the <emphasis remap='I'>blockPtr</emphasis> parameter with values
that describe the address and layout of the image data that the
photo image has stored internally.  The values are valid
until the image is destroyed or its size is changed.
<function>Tk_PhotoGetImage</function> returns 1 for compatibility with the
corresponding procedure in the old photo widget.</para>

<para><function>Tk_PhotoBlank</function> blanks the entire area of the
photo image.  Blank areas of a photo image are transparent.</para>

<para><function>Tk_PhotoExpand</function> requests that the widget's image be expanded to be
at least <emphasis remap='I'>width</emphasis> x <emphasis remap='I'>height</emphasis> pixels in size.  The width and/or
height are unchanged if the user has specified an explicit image width
or height with the <option>-width</option> and/or <option>-height</option> configuration
options, respectively.
If the image data
are being supplied in many small blocks, it is more efficient to use
<function>Tk_PhotoExpand</function> or <function>Tk_PhotoSetSize</function> at the beginning rather than
allowing the image to expand in many small increments as image blocks
are supplied.</para>

<!-- .VS 8.5 -->
<para><function>Tk_PhotoExpand</function> normally returns <emphasis remap='B'>TCL_OK</emphasis>, though if it cannot
allocate sufficient memory to hold the resulting image, <emphasis remap='B'>TCL_ERROR</emphasis> is
returned instead and, if the <emphasis remap='I'>interp</emphasis> argument is non-NULL, an
error message is placed in the interpreter's result.</para>
<!-- .VE 8.5 -->

<para><function>Tk_PhotoSetSize</function> specifies the size of the image, as if the user
had specified the given <emphasis remap='I'>width</emphasis> and <emphasis remap='I'>height</emphasis> values to the
<option>-width</option> and <option>-height</option> configuration options.  A value of
zero for <emphasis remap='I'>width</emphasis> or <emphasis remap='I'>height</emphasis> does not change the image's width
or height, but allows the width or height to be changed by subsequent
calls to <function>Tk_PhotoPutBlock</function>, <function>Tk_PhotoPutZoomedBlock</function> or
<function>Tk_PhotoExpand</function>.</para>

<!-- .VS 8.5 -->
<para><function>Tk_PhotoSetSize</function> normally returns <emphasis remap='B'>TCL_OK</emphasis>, though if it cannot
allocate sufficient memory to hold the resulting image, <emphasis remap='B'>TCL_ERROR</emphasis> is
returned instead and, if the <emphasis remap='I'>interp</emphasis> argument is non-NULL, an
error message is placed in the interpreter's result.</para>
<!-- .VE 8.5 -->

<para><function>Tk_PhotoGetSize</function> returns the dimensions of the image in
*<emphasis remap='I'>widthPtr</emphasis> and *<emphasis remap='I'>heightPtr</emphasis>.</para>

</refsect1>

<refsect1 xml:id='portability'><title>PORTABILITY</title>
<para>In Tk 8.3 and earlier, <function>Tk_PhotoPutBlock</function> and
<function>Tk_PhotoPutZoomedBlock</function> had different signatures. If you want to
compile code that uses the old interface against 8.4 without updating
your code, compile it with the flag
-DUSE_COMPOSITELESS_PHOTO_PUT_BLOCK.  Code linked using Stubs against
older versions of Tk will continue to work.</para>

<!-- .VS 8.5 -->
<para>In Tk 8.4, <function>Tk_PhotoPutBlock</function>, <function>Tk_PhotoPutZoomedBlock</function>,
<function>Tk_PhotoExpand</function> and <function>Tk_PhotoSetSize</function> did not take an
<emphasis remap='I'>interp</emphasis> argument or return any result code.  If insufficient
memory was available for an image, Tk would panic.  This behaviour is
still supported if you compile your extension with the additional flag
-DUSE_PANIC_ON_PHOTO_ALLOC_FAILURE.  Code linked using Stubs against
older versions of Tk will continue to work.</para>
<!-- .VE 8.5 -->

</refsect1>

<refsect1 xml:id='credits'><title>CREDITS</title>
<para>The code for the photo image type was developed by Paul Mackerras,
based on his earlier photo widget code.</para>

</refsect1>

<refsect1 xml:id='keywords'><title>KEYWORDS</title>
<para>photo, image</para>
</refsect1>
</refentry>

