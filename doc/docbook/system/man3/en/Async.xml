<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man+tkman by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='async3'>

<!-- '\" Copyright (c) 1989\-1993 The Regents of the University of California.
'\" Copyright (c) 1994\-1996 Sun Microsystems, Inc. -->

<!-- '\" See the file "license.terms" for information on usage and redistribution
'\" of this file, and for a DISCLAIMER OF ALL WARRANTIES.
'\" 
'\" RCS: @(#) $Id$
'\"  -->
<refentryinfo><date>7.0</date></refentryinfo>
<refmeta>
<refentrytitle>Tcl_AsyncCreate</refentrytitle>
<manvolnum>3</manvolnum>
<refmiscinfo class='date'>7.0</refmiscinfo>
<refmiscinfo class='source'>Tcl</refmiscinfo>
<refmiscinfo class='manual'>Tcl Library Procedures</refmiscinfo>
</refmeta>
<refnamediv>
<refname>Tcl_AsyncCreate</refname>
<refname>Tcl_AsyncMark</refname>
<refname>Tcl_AsyncInvoke</refname>
<refname>Tcl_AsyncDelete</refname>
<refname>Tcl_AsyncReady</refname>
<refpurpose>handle asynchronous events</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<funcsynopsis>
<funcsynopsisinfo>
#include &lt;tcl.h&gt;

</funcsynopsisinfo>
<funcprototype>
<funcdef>Tcl_AsyncHandler <function>Tcl_AsyncCreate</function></funcdef>
<paramdef><parameter>proc</parameter></paramdef>
<paramdef><parameter>clientData</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_AsyncMark</function></funcdef>
<paramdef><parameter>async</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_AsyncInvoke</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
<paramdef><parameter>code</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_AsyncDelete</function></funcdef>
<paramdef><parameter>async</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_AsyncReady</function></funcdef>
<void/></funcprototype>
</funcsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='arguments'><title>ARGUMENTS</title>
<!-- .AS Tcl_AsyncHandler clientData -->
<informaltable>
<tgroup cols='3'>
<tbody>

<row><entry>"Tcl_AsyncProc"</entry><entry>"*proc"</entry><entry>"in"</entry>
<entry>
<para>Procedure to invoke to handle an asynchronous event.</para>
</entry>
</row>
<row><entry>ClientData</entry><entry>clientData</entry><entry>in</entry>
<entry>
<para>One-word value to pass to <emphasis remap='I'>proc</emphasis>.</para>
</entry>
</row>
<row><entry>Tcl_AsyncHandler</entry><entry>async</entry><entry>in</entry>
<entry>
<para>Token for asynchronous event handler.</para>
</entry>
</row>
<row><entry>Tcl_Interp</entry><entry>*interp</entry><entry>in</entry>
<entry>
<para>Tcl interpreter in which command was being evaluated when handler was
invoked, or NULL if handler was invoked when there was no interpreter
active.</para>
</entry>
</row>
<row><entry>int</entry><entry>code</entry><entry>in</entry>
<entry>
<para>Completion code from command that just completed in <varname role='parameter'>interp</varname>,
or 0 if <varname role='parameter'>interp</varname> is NULL.</para>
<!-- .BE -->
</entry>
</row>
</tbody>
</tgroup>
</informaltable>

</refsect1>

<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para>These procedures provide a safe mechanism for dealing with
asynchronous events such as signals.
If an event such as a signal occurs while a Tcl script is being
evaluated then it is not safe to take any substantive action to
process the event.
For example, it is not safe to evaluate a Tcl script since the
interpreter may already be in the middle of evaluating a script;
it may not even be safe to allocate memory, since a memory
allocation could have been in progress when the event occurred.
The only safe approach is to set a flag indicating that the event
occurred, then handle the event later when the world has returned
to a clean state, such as after the current Tcl command completes.</para>

<para><function>Tcl_AsyncCreate</function>, <function>Tcl_AsyncDelete</function>, and <function>Tcl_AsyncReady</function>
are thread sensitive.  They access and/or set a thread-specific data
structure in the event of a core built with <option>--enable-threads</option>.  The token
created by <function>Tcl_AsyncCreate</function> contains the needed thread information it
was called from so that calling <function>Tcl_AsyncMark</function>(<emphasis remap='I'>token</emphasis>) will only yield
the origin thread into the asynchronous handler.</para>

<para><function>Tcl_AsyncCreate</function> creates an asynchronous handler and returns
a token for it.
The asynchronous handler must be created before
any occurrences of the asynchronous event that it is intended
to handle (it is not safe to create a handler at the time of
an event).
When an asynchronous event occurs the code that detects the event
(such as a signal handler) should call <function>Tcl_AsyncMark</function> with the
token for the handler.
<function>Tcl_AsyncMark</function> will mark the handler as ready to execute, but it
will not invoke the handler immediately.
Tcl will call the <emphasis remap='I'>proc</emphasis> associated with the handler later, when
the world is in a safe state, and <emphasis remap='I'>proc</emphasis> can then carry out
the actions associated with the asynchronous event.
<emphasis remap='I'>Proc</emphasis> should have arguments and result that match the
type <function>Tcl_AsyncProc</function>:</para>
<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_AsyncProc</function></funcdef>
    <paramdef>ClientData <parameter>clientData</parameter></paramdef>
    <paramdef>Tcl_Interp * <parameter>interp</parameter></paramdef>
    <paramdef>int <parameter>code</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para>The <varname role='parameter'>clientData</varname> will be the same as the <varname role='parameter'>clientData</varname>
argument passed to <function>Tcl_AsyncCreate</function> when the handler was
created.
If <emphasis remap='I'>proc</emphasis> is invoked just after a command has completed
execution in an interpreter, then <varname role='parameter'>interp</varname> will identify
the interpreter in which the command was evaluated and
<varname role='parameter'>code</varname> will be the completion code returned by that
command.
The command's result will be present in the interpreter's result.
When <emphasis remap='I'>proc</emphasis> returns, whatever it leaves in the interpreter's result
will be returned as the result of the command and the integer
value returned by <emphasis remap='I'>proc</emphasis> will be used as the new completion
code for the command.</para>

<para>It is also possible for <emphasis remap='I'>proc</emphasis> to be invoked when no interpreter
is active.
This can happen, for example, if an asynchronous event occurs while
the application is waiting for interactive input or an X event.
In this case <varname role='parameter'>interp</varname> will be NULL and <varname role='parameter'>code</varname> will be
0, and the return value from <emphasis remap='I'>proc</emphasis> will be ignored.</para>

<para>The procedure <function>Tcl_AsyncInvoke</function> is called to invoke all of the
handlers that are ready.
The procedure <function>Tcl_AsyncReady</function> will return non-zero whenever any
asynchronous handlers are ready;  it can be checked to avoid calls
to <function>Tcl_AsyncInvoke</function> when there are no ready handlers.
Tcl calls <function>Tcl_AsyncReady</function> after each command is evaluated
and calls <function>Tcl_AsyncInvoke</function> if needed.
Applications may also call <function>Tcl_AsyncInvoke</function> at interesting
times for that application.
For example, Tcl's event handler calls <function>Tcl_AsyncReady</function>
after each event and calls <function>Tcl_AsyncInvoke</function> if needed.
The <varname role='parameter'>interp</varname> and <varname role='parameter'>code</varname> arguments to <function>Tcl_AsyncInvoke</function>
have the same meaning as for <emphasis remap='I'>proc</emphasis>:  they identify the active
interpreter, if any, and the completion code from the command
that just completed.</para>

<para><function>Tcl_AsyncDelete</function> removes an asynchronous handler so that
its <emphasis remap='I'>proc</emphasis> will never be invoked again.
A handler can be deleted even when ready, and it will still
not be invoked.</para>

<para>If multiple handlers become active at the same time, the
handlers are invoked in the order they were created (oldest
handler first).
The <varname role='parameter'>code</varname> and the interpreter's result for later handlers
reflect the values returned by earlier handlers, so that
the most recently created handler has last say about
the interpreter's result and completion code.
If new handlers become ready while handlers are executing,
<function>Tcl_AsyncInvoke</function> will invoke them all;  at each point it
invokes the highest-priority (oldest) ready handler, repeating
this over and over until there are no longer any ready handlers.</para>
</refsect1>

<refsect1 xml:id='warning'><title>WARNING</title>
<para>It is almost always a bad idea for an asynchronous event
handler to modify the interpreter's result or return a code different
from its <varname role='parameter'>code</varname> argument.
This sort of behavior can disrupt the execution of scripts in
subtle ways and result in bugs that are extremely difficult
to track down.
If an asynchronous event handler needs to evaluate Tcl scripts
then it should first save the interpreter's state by calling
<emphasis remap='B'>Tcl_SaveInterpState</emphasis>, passing in the <varname role='parameter'>code</varname> argument.
When the asynchronous handler is finished it should restore
the interpreter's state by calling <emphasis remap='B'>Tcl_RestoreInterpState</emphasis>,
and then returning the <varname role='parameter'>code</varname> argument.</para>

</refsect1>

<refsect1 xml:id='keywords'><title>KEYWORDS</title>
<para>asynchronous event, handler, signal, Tcl_SaveInterpState, thread</para>
</refsect1>
</refentry>

