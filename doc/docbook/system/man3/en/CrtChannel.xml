<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man+tkman by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='crtchannel3'>

<!-- '\" Copyright (c) 1996\-1997 Sun Microsystems, Inc.
'\" Copyright (c) 1997\-2000 Ajuba Solutions. -->

<!-- '\" See the file "license.terms" for information on usage and redistribution
'\" of this file, and for a DISCLAIMER OF ALL WARRANTIES. -->

<!-- '\" RCS: @(#) $Id$ -->
<refentryinfo><date>8.4</date></refentryinfo>
<refmeta>
<refentrytitle>Tcl_CreateChannel</refentrytitle>
<manvolnum>3</manvolnum>
<refmiscinfo class='date'>8.4</refmiscinfo>
<refmiscinfo class='source'>Tcl</refmiscinfo>
<refmiscinfo class='manual'>Tcl Library Procedures</refmiscinfo>
</refmeta>
<refnamediv>
<refname>Tcl_CreateChannel</refname>
<refname>Tcl_GetChannelInstanceData</refname>
<refname>Tcl_GetChannelType</refname>
<refname>Tcl_GetChannelName</refname>
<refname>Tcl_GetChannelHandle</refname>
<refname>Tcl_GetChannelMode</refname>
<refname>Tcl_GetChannelBufferSize</refname>
<refname>Tcl_SetChannelBufferSize</refname>
<refname>Tcl_NotifyChannel</refname>
<refname>Tcl_BadChannelOption</refname>
<refname>Tcl_ChannelName</refname>
<refname>Tcl_ChannelVersion</refname>
<refname>Tcl_ChannelBlockModeProc</refname>
<refname>Tcl_ChannelCloseProc</refname>
<refname>Tcl_ChannelClose2Proc</refname>
<refname>Tcl_ChannelInputProc</refname>
<refname>Tcl_ChannelOutputProc</refname>
<refname>Tcl_ChannelSeekProc</refname>
<refname>Tcl_ChannelWideSeekProc</refname>
<refname>Tcl_ChannelTruncateProc</refname>
<refname>Tcl_ChannelSetOptionProc</refname>
<refname>Tcl_ChannelGetOptionProc</refname>
<refname>Tcl_ChannelWatchProc</refname>
<refname>Tcl_ChannelGetHandleProc</refname>
<refname>Tcl_ChannelFlushProc</refname>
<refname>Tcl_ChannelHandlerProc</refname>
<refname>Tcl_ChannelThreadActionProc</refname>
<refname>Tcl_IsChannelShared</refname>
<refname>Tcl_IsChannelRegistered</refname>
<refname>Tcl_CutChannel</refname>
<refname>Tcl_SpliceChannel</refname>
<refname>Tcl_IsChannelExisting</refname>
<refname>Tcl_ClearChannelHandlers</refname>
<refname>Tcl_GetChannelThread</refname>
<refname>Tcl_ChannelBuffered</refname>
<refpurpose>procedures for creating and manipulating channels</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<funcsynopsis>
<funcsynopsisinfo>
#include &lt;tcl.h&gt;

</funcsynopsisinfo>
<funcprototype>
<funcdef>Tcl_Channel <function>Tcl_CreateChannel</function></funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
<paramdef><parameter>channelName</parameter></paramdef>
<paramdef><parameter>instanceData</parameter></paramdef>
<paramdef><parameter>mask</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>ClientData <function>Tcl_GetChannelInstanceData</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_ChannelType</function> *Tcl_GetChannelType</funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>const char *<function>Tcl_GetChannelName</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_GetChannelHandle</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
<paramdef><parameter>direction</parameter></paramdef>
<paramdef><parameter>handlePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>Tcl_ThreadId <function>Tcl_GetChannelThread</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_GetChannelMode</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_GetChannelBufferSize</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_SetChannelBufferSize</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
<paramdef><parameter>size</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_NotifyChannel</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
<paramdef><parameter>mask</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_BadChannelOption</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
<paramdef><parameter>optionName</parameter></paramdef>
<paramdef><parameter>optionList</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_IsChannelShared</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_IsChannelRegistered</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_IsChannelExisting</function></funcdef>
<paramdef><parameter>channelName</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tcl_CutChannel</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tcl_SpliceChannel</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>void <function>Tcl_ClearChannelHandlers</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>int <function>Tcl_ChannelBuffered</function></funcdef>
<paramdef><parameter>channel</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>const char *<function>Tcl_ChannelName</function></funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef>Tcl_ChannelTypeVersion <function>Tcl_ChannelVersion</function></funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverBlockModeProc</function> *Tcl_ChannelBlockModeProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverCloseProc</function> *Tcl_ChannelCloseProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverClose2Proc</function> *Tcl_ChannelClose2Proc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverInputProc</function> *Tcl_ChannelInputProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverOutputProc</function> *Tcl_ChannelOutputProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverSeekProc</function> *Tcl_ChannelSeekProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverWideSeekProc</function> *Tcl_ChannelWideSeekProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverThreadActionProc</function> *Tcl_ChannelThreadActionProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverTruncateProc</function> *Tcl_ChannelTruncateProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverSetOptionProc</function> *Tcl_ChannelSetOptionProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverGetOptionProc</function> *Tcl_ChannelGetOptionProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverWatchProc</function> *Tcl_ChannelWatchProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverGetHandleProc</function> *Tcl_ChannelGetHandleProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverFlushProc</function> *Tcl_ChannelFlushProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
<funcprototype>
<funcdef><function>Tcl_DriverHandlerProc</function> *Tcl_ChannelHandlerProc</funcdef>
<paramdef><parameter>typePtr</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='arguments'><title>ARGUMENTS</title>
<!-- .AS const Tcl_ChannelType *channelName -->
<informaltable>
<tgroup cols='3'>
<tbody>

<row><entry>"const Tcl_ChannelType"</entry><entry>"*typePtr"</entry><entry>"in"</entry>
<entry>
<para>Points to a structure containing the addresses of procedures that
can be called to perform I/O and other functions on the channel.</para>
</entry>
</row>
<row><entry>"const char"</entry><entry>*channelName</entry><entry>in</entry>
<entry>
<para>The name of this channel, such as <emphasis remap='B'>file3</emphasis>; must not be in use
by any other channel. Can be NULL, in which case the channel is
created without a name.</para>
</entry>
</row>
<row><entry>ClientData</entry><entry>instanceData</entry><entry>in</entry>
<entry>
<para>Arbitrary one-word value to be associated with this channel.  This
value is passed to procedures in <emphasis remap='I'>typePtr</emphasis> when they are invoked.</para>
</entry>
</row>
<row><entry>int</entry><entry>mask</entry><entry>in</entry>
<entry>
<para>OR-ed combination of <emphasis remap='B'>TCL_READABLE</emphasis> and <emphasis remap='B'>TCL_WRITABLE</emphasis> to indicate
whether a channel is readable and writable.</para>
</entry>
</row>
<row><entry>Tcl_Channel</entry><entry>channel</entry><entry>in</entry>
<entry>
<para>The channel to operate on.</para>
</entry>
</row>
<row><entry>int</entry><entry>direction</entry><entry>in</entry>
<entry>
<para><emphasis remap='B'>TCL_READABLE</emphasis> means the input handle is wanted; <emphasis remap='B'>TCL_WRITABLE</emphasis>
means the output handle is wanted.</para>
</entry>
</row>
<row><entry>ClientData</entry><entry>*handlePtr</entry><entry>out</entry>
<entry>
<para>Points to the location where the desired OS-specific handle should be
stored.</para>
</entry>
</row>
<row><entry>int</entry><entry>size</entry><entry>in</entry>
<entry>
<para>The size, in bytes, of buffers to allocate in this channel.</para>
</entry>
</row>
<row><entry>int</entry><entry>mask</entry><entry>in</entry>
<entry>
<para>An OR-ed combination of <emphasis remap='B'>TCL_READABLE</emphasis>, <emphasis remap='B'>TCL_WRITABLE</emphasis>
and <emphasis remap='B'>TCL_EXCEPTION</emphasis> that indicates events that have occurred on
this channel.</para>
</entry>
</row>
<row><entry>Tcl_Interp</entry><entry>*interp</entry><entry>in</entry>
<entry>
<para>Current interpreter. (can be NULL)</para>
</entry>
</row>
<row><entry>"const char"</entry><entry>*optionName</entry><entry>in</entry>
<entry>
<para>Name of the invalid option.</para>
</entry>
</row>
<row><entry>"const char"</entry><entry>*optionList</entry><entry>in</entry>
<entry>
<para>Specific options list (space separated words, without
<quote>-</quote>)
to append to the standard generic options list.
Can be NULL for generic options error message only.</para>

<!-- .BE -->
</entry>
</row>
</tbody>
</tgroup>
</informaltable>

</refsect1>

<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para>Tcl uses a two-layered channel architecture. It provides a generic upper
layer to enable C and Tcl programs to perform input and output using the
same APIs for a variety of files, devices, sockets etc. The generic C APIs
are described in the manual entry for <emphasis remap='B'>Tcl_OpenFileChannel</emphasis>.</para>

<para>The lower layer provides type-specific channel drivers for each type
of device supported on each platform.  This manual entry describes the
C APIs used to communicate between the generic layer and the
type-specific channel drivers.  It also explains how new types of
channels can be added by providing new channel drivers.</para>

<para>Channel drivers consist of a number of components: First, each channel
driver provides a <function>Tcl_ChannelType</function> structure containing pointers to
functions implementing the various operations used by the generic layer to
communicate with the channel driver. The <function>Tcl_ChannelType</function> structure
and the functions referenced by it are described in the section
<emphasis remap='B'>TCL_CHANNELTYPE</emphasis>, below.</para>

<para>Second, channel drivers usually provide a Tcl command to create
instances of that type of channel. For example, the Tcl <emphasis remap='B'>open</emphasis>
command creates channels that use the file and command channel
drivers, and the Tcl <emphasis remap='B'>socket</emphasis> command creates channels that use
TCP sockets for network communication.</para>

<para>Third, a channel driver optionally provides a C function to open
channel instances of that type. For example, <emphasis remap='B'>Tcl_OpenFileChannel</emphasis>
opens a channel that uses the file channel driver, and
<emphasis remap='B'>Tcl_OpenTcpClient</emphasis> opens a channel that uses the TCP network
protocol.  These creation functions typically use
<function>Tcl_CreateChannel</function> internally to open the channel.</para>

<para>To add a new type of channel you must implement a C API or a Tcl command
that opens a channel by invoking <function>Tcl_CreateChannel</function>.
When your driver calls <function>Tcl_CreateChannel</function> it passes in
a <function>Tcl_ChannelType</function> structure describing the driver's I/O
procedures.
The generic layer will then invoke the functions referenced in that
structure to perform operations on the channel.</para>

<para><function>Tcl_CreateChannel</function> opens a new channel and associates the supplied
<emphasis remap='I'>typePtr</emphasis> and <varname role='parameter'>instanceData</varname> with it. The channel is opened in the
mode indicated by <varname role='parameter'>mask</varname>.
For a discussion of channel drivers, their operations and the
<function>Tcl_ChannelType</function> structure, see the section <emphasis remap='B'>TCL_CHANNELTYPE</emphasis>, below.</para>

<para><function>Tcl_CreateChannel</function> interacts with the code managing the standard
channels. Once a standard channel was initialized either through a
call to <emphasis remap='B'>Tcl_GetStdChannel</emphasis> or a call to <emphasis remap='B'>Tcl_SetStdChannel</emphasis>
closing this standard channel will cause the next call to
<function>Tcl_CreateChannel</function> to make the new channel the new standard
channel too. See <emphasis remap='B'>Tcl_StandardChannels</emphasis> for a general treatise
about standard channels and the behaviour of the Tcl library with
regard to them.</para>

<para><function>Tcl_GetChannelInstanceData</function> returns the instance data associated with
the channel in <emphasis remap='I'>channel</emphasis>. This is the same as the <varname role='parameter'>instanceData</varname>
argument in the call to <function>Tcl_CreateChannel</function> that created this channel.</para>

<para><emphasis remap='B'>Tcl_GetChannelType</emphasis> returns a pointer to the <function>Tcl_ChannelType</function>
structure used by the channel in the <emphasis remap='I'>channel</emphasis> argument. This is
the same as the <emphasis remap='I'>typePtr</emphasis> argument in the call to
<function>Tcl_CreateChannel</function> that created this channel.</para>

<para><function>Tcl_GetChannelName</function> returns a string containing the name associated
with the channel, or NULL if the <emphasis remap='I'>channelName</emphasis> argument to
<function>Tcl_CreateChannel</function> was NULL.</para>

<para><function>Tcl_GetChannelHandle</function> places the OS-specific device handle
associated with <emphasis remap='I'>channel</emphasis> for the given <varname role='parameter'>direction</varname> in the
location specified by <varname role='parameter'>handlePtr</varname> and returns <emphasis remap='B'>TCL_OK</emphasis>.  If
the channel does not have a device handle for the specified direction,
then <emphasis remap='B'>TCL_ERROR</emphasis> is returned instead.  Different channel drivers
will return different types of handle.  Refer to the manual entries
for each driver to determine what type of handle is returned.</para>

<para><function>Tcl_GetChannelThread</function> returns the id of the thread currently managing
the specified <emphasis remap='I'>channel</emphasis>. This allows channel drivers to send their file
events to the correct event queue even for a multi-threaded core.</para>

<para><function>Tcl_GetChannelMode</function> returns an OR-ed combination of <emphasis remap='B'>TCL_READABLE</emphasis>
and <emphasis remap='B'>TCL_WRITABLE</emphasis>, indicating whether the channel is open for input
and output.</para>

<para><function>Tcl_GetChannelBufferSize</function> returns the size, in bytes, of buffers
allocated to store input or output in <emphasis remap='I'>channel</emphasis>. If the value was not set
by a previous call to <function>Tcl_SetChannelBufferSize</function>, described below, then
the default value of 4096 is returned.</para>

<para><function>Tcl_SetChannelBufferSize</function> sets the size, in bytes, of buffers that
will be allocated in subsequent operations on the channel to store input or
output. The <emphasis remap='I'>size</emphasis> argument should be between ten and one million,
allowing buffers of ten bytes to one million bytes. If <emphasis remap='I'>size</emphasis> is
outside this range, <function>Tcl_SetChannelBufferSize</function> sets the buffer size to
4096.</para>

<para><function>Tcl_NotifyChannel</function> is called by a channel driver to indicate to
the generic layer that the events specified by <varname role='parameter'>mask</varname> have
occurred on the channel.  Channel drivers are responsible for invoking
this function whenever the channel handlers need to be called for the
channel.  See <emphasis remap='B'>WATCHPROC</emphasis> below for more details.</para>

<para><function>Tcl_BadChannelOption</function> is called from driver specific
<emphasis remap='I'>setOptionProc</emphasis> or <emphasis remap='I'>getOptionProc</emphasis> to generate a complete
error message.</para>

<para><function>Tcl_ChannelBuffered</function> returns the number of bytes of input
currently buffered in the internal buffer (push back area) of the
channel itself. It does not report about the data in the overall
buffers for the stack of channels the supplied channel is part of.</para>

<para><function>Tcl_IsChannelShared</function> checks the refcount of the specified
<emphasis remap='I'>channel</emphasis> and returns whether the <emphasis remap='I'>channel</emphasis> was shared among
multiple interpreters (result == 1) or not (result == 0).</para>

<para><function>Tcl_IsChannelRegistered</function> checks whether the specified <emphasis remap='I'>channel</emphasis> is
registered in the given <varname role='parameter'>interp</varname>reter (result == 1) or not
(result == 0).</para>

<para><function>Tcl_IsChannelExisting</function> checks whether a channel with the specified
name is registered in the (thread)-global list of all channels (result
== 1) or not (result == 0).</para>

<para><function>Tcl_CutChannel</function> removes the specified <emphasis remap='I'>channel</emphasis> from the
(thread)global list of all channels (of the current thread).
Application to a channel still registered in some interpreter
is not allowed.
<!-- .VS 8.5 -->
Also notifies the driver if the <function>Tcl_ChannelType</function> version is
<emphasis remap='B'>TCL_CHANNEL_VERSION_4</emphasis> (or higher), and
<function>Tcl_DriverThreadActionProc</function> is defined for it.</para>
<!-- .VE 8.5 -->

<para><function>Tcl_SpliceChannel</function> adds the specified <emphasis remap='I'>channel</emphasis> to the
(thread)global list of all channels (of the current thread).
Application to a channel registered in some interpreter is not allowed.
<!-- .VS 8.5 -->
Also notifies the driver if the <function>Tcl_ChannelType</function> version is
<emphasis remap='B'>TCL_CHANNEL_VERSION_4</emphasis> (or higher), and
<function>Tcl_DriverThreadActionProc</function> is defined for it.</para>
<!-- .VE 8.5 -->

<para><function>Tcl_ClearChannelHandlers</function> removes all channel handlers and event
scripts associated with the specified <emphasis remap='I'>channel</emphasis>, thus shutting
down all event processing for this channel.</para>
</refsect1>

<refsect1 xml:id='tcl_channeltype'><title>TCL_CHANNELTYPE</title>
<para>A channel driver provides a <function>Tcl_ChannelType</function> structure that contains
pointers to functions that implement the various operations on a channel;
these operations are invoked as needed by the generic layer.  The structure
was versioned starting in Tcl 8.3.2/8.4 to correct a problem with stacked
channel drivers.  See the <emphasis remap='B'>OLD CHANNEL TYPES</emphasis> section below for
details about the old structure.</para>

<para>The <function>Tcl_ChannelType</function> structure contains the following fields:</para>
<programlisting remap='CS'>
typedef struct Tcl_ChannelType {
        char *<emphasis remap='I'>typeName</emphasis>;
        Tcl_ChannelTypeVersion <emphasis remap='I'>version</emphasis>;
        Tcl_DriverCloseProc *<emphasis remap='I'>closeProc</emphasis>;
        Tcl_DriverInputProc *<emphasis remap='I'>inputProc</emphasis>;
        Tcl_DriverOutputProc *<emphasis remap='I'>outputProc</emphasis>;
        Tcl_DriverSeekProc *<emphasis remap='I'>seekProc</emphasis>;
        Tcl_DriverSetOptionProc *<emphasis remap='I'>setOptionProc</emphasis>;
        Tcl_DriverGetOptionProc *<emphasis remap='I'>getOptionProc</emphasis>;
        Tcl_DriverWatchProc *<emphasis remap='I'>watchProc</emphasis>;
        Tcl_DriverGetHandleProc *<emphasis remap='I'>getHandleProc</emphasis>;
        Tcl_DriverClose2Proc *<emphasis remap='I'>close2Proc</emphasis>;
        Tcl_DriverBlockModeProc *<emphasis remap='I'>blockModeProc</emphasis>;
        Tcl_DriverFlushProc *<emphasis remap='I'>flushProc</emphasis>;
        Tcl_DriverHandlerProc *<emphasis remap='I'>handlerProc</emphasis>;
        Tcl_DriverWideSeekProc *<emphasis remap='I'>wideSeekProc</emphasis>;
        Tcl_DriverThreadActionProc *<emphasis remap='I'>threadActionProc</emphasis>;
        Tcl_DriverTruncateProc *<emphasis remap='I'>truncateProc</emphasis>;
} Tcl_ChannelType;
</programlisting> <!-- remap='CE' -->

<para>It is not necessary to provide implementations for all channel
operations.  Those which are not necessary may be set to NULL in the
struct: <emphasis remap='I'>blockModeProc</emphasis>, <emphasis remap='I'>seekProc</emphasis>, <emphasis remap='I'>setOptionProc</emphasis>,
<emphasis remap='I'>getOptionProc</emphasis>, and <emphasis remap='I'>close2Proc</emphasis>, in addition to
<emphasis remap='I'>flushProc</emphasis>, <emphasis remap='I'>handlerProc</emphasis>, <emphasis remap='I'>threadActionProc</emphasis>, and
<emphasis remap='I'>truncateProc</emphasis>.  Other functions that cannot be implemented in a
meaningful way should return <errorcode>EINVAL</errorcode> when called, to indicate
that the operations they represent are not available. Also note that
<emphasis remap='I'>wideSeekProc</emphasis> can be NULL if <emphasis remap='I'>seekProc</emphasis> is.</para>

<para>The user should only use the above structure for <function>Tcl_ChannelType</function>
instantiation.  When referencing fields in a <function>Tcl_ChannelType</function>
structure, the following functions should be used to obtain the values:
<function>Tcl_ChannelName</function>, <function>Tcl_ChannelVersion</function>,
<emphasis remap='B'>Tcl_ChannelBlockModeProc</emphasis>, <emphasis remap='B'>Tcl_ChannelCloseProc</emphasis>,
<emphasis remap='B'>Tcl_ChannelClose2Proc</emphasis>, <emphasis remap='B'>Tcl_ChannelInputProc</emphasis>,
<emphasis remap='B'>Tcl_ChannelOutputProc</emphasis>, <emphasis remap='B'>Tcl_ChannelSeekProc</emphasis>,
<emphasis remap='B'>Tcl_ChannelWideSeekProc</emphasis>, <emphasis remap='B'>Tcl_ChannelThreadActionProc</emphasis>,
<!-- .VS 8.5 -->
<emphasis remap='B'>Tcl_ChannelTruncateProc</emphasis>,
<!-- .VE 8.5 -->
<emphasis remap='B'>Tcl_ChannelSetOptionProc</emphasis>, <emphasis remap='B'>Tcl_ChannelGetOptionProc</emphasis>,
<emphasis remap='B'>Tcl_ChannelWatchProc</emphasis>, <emphasis remap='B'>Tcl_ChannelGetHandleProc</emphasis>,
<emphasis remap='B'>Tcl_ChannelFlushProc</emphasis>, or <emphasis remap='B'>Tcl_ChannelHandlerProc</emphasis>.</para>

<para>The change to the structures was made in such a way that standard channel
types are binary compatible.  However, channel types that use stacked
channels (i.e. TLS, Trf) have new versions to correspond to the above change
since the previous code for stacked channels had problems.</para>

<refsect2 xml:id='typename'><title>TYPENAME</title>

<para>The <emphasis remap='I'>typeName</emphasis> field contains a null-terminated string that
identifies the type of the device implemented by this driver, e.g.
<emphasis remap='B'>file</emphasis> or <emphasis remap='B'>socket</emphasis>.</para>

<para>This value can be retrieved with <function>Tcl_ChannelName</function>, which returns
a pointer to the string.</para>
</refsect2>

<refsect2 xml:id='version'><title>VERSION</title>


<para>The <emphasis remap='I'>version</emphasis> field should be set to the version of the structure
that you require. <emphasis remap='B'>TCL_CHANNEL_VERSION_2</emphasis> is the minimum recommended.
<emphasis remap='B'>TCL_CHANNEL_VERSION_3</emphasis> must be set to specify the <emphasis remap='I'>wideSeekProc</emphasis> member.
<emphasis remap='B'>TCL_CHANNEL_VERSION_4</emphasis> must be set to specify the <emphasis remap='I'>threadActionProc</emphasis> member
(includes <emphasis remap='I'>wideSeekProc</emphasis>).
<!-- .VS 8.5 -->
<emphasis remap='B'>TCL_CHANNEL_VERSION_5</emphasis> must be set to specify the
<emphasis remap='I'>truncateProc</emphasis> members (includes
<emphasis remap='I'>wideSeekProc</emphasis> and <emphasis remap='I'>threadActionProc</emphasis>).
<!-- .VE 8.5 -->
If it is not set to any of these, then this
<function>Tcl_ChannelType</function> is assumed to have the original structure.  See
<emphasis remap='B'>OLD CHANNEL TYPES</emphasis> for more details.  While Tcl will recognize
and function with either structures, stacked channels must be of at
least <emphasis remap='B'>TCL_CHANNEL_VERSION_2</emphasis> to function correctly.</para>

<para>This value can be retrieved with <function>Tcl_ChannelVersion</function>, which returns
one of
<!-- .VS 8.5 -->
<emphasis remap='B'>TCL_CHANNEL_VERSION_5</emphasis>,
<!-- .VE 8.5 -->
<emphasis remap='B'>TCL_CHANNEL_VERSION_4</emphasis>,
<emphasis remap='B'>TCL_CHANNEL_VERSION_3</emphasis>,
<emphasis remap='B'>TCL_CHANNEL_VERSION_2</emphasis> or <emphasis remap='B'>TCL_CHANNEL_VERSION_1</emphasis>.</para>
</refsect2>

<refsect2 xml:id='blockmodeproc'><title>BLOCKMODEPROC</title>

<para>The <emphasis remap='I'>blockModeProc</emphasis> field contains the address of a function called by
the generic layer to set blocking and nonblocking mode on the device.
<emphasis remap='I'>BlockModeProc</emphasis> should match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverBlockModeProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>int <parameter>mode</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para>The <varname role='parameter'>instanceData</varname> is the same as the value passed to
<function>Tcl_CreateChannel</function> when this channel was created.  The <varname role='parameter'>mode</varname>
argument is either <emphasis remap='B'>TCL_MODE_BLOCKING</emphasis> or <emphasis remap='B'>TCL_MODE_NONBLOCKING</emphasis> to
set the device into blocking or nonblocking mode. The function should
return zero if the operation was successful, or a nonzero POSIX error code
if the operation failed.</para>

<para>If the operation is successful, the function can modify the supplied
<varname role='parameter'>instanceData</varname> to record that the channel entered blocking or
nonblocking mode and to implement the blocking or nonblocking behavior.
For some device types, the blocking and nonblocking behavior can be
implemented by the underlying operating system; for other device types, the
behavior must be emulated in the channel driver.</para>

<para>This value can be retrieved with <emphasis remap='B'>Tcl_ChannelBlockModeProc</emphasis>, which returns
a pointer to the function.</para>

<para>A channel driver <emphasis remap='B'>not</emphasis> supplying a <emphasis remap='I'>blockModeProc</emphasis> has to be
very, very careful. It has to tell the generic layer exactly which
blocking mode is acceptable to it, and should this also document for
the user so that the blocking mode of the channel is not changed to an
unacceptable value. Any confusion here may lead the interpreter into a
(spurious and difficult to find) deadlock.</para>
</refsect2>

<refsect2 xml:id='closeproc_and_close2proc'><title>CLOSEPROC AND CLOSE2PROC</title>

<para>The <emphasis remap='I'>closeProc</emphasis> field contains the address of a function called by the
generic layer to clean up driver-related information when the channel is
closed. <emphasis remap='I'>CloseProc</emphasis> must match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverCloseProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>Tcl_Interp * <parameter>interp</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para>The <varname role='parameter'>instanceData</varname> argument is the same as the value provided to
<function>Tcl_CreateChannel</function> when the channel was created. The function should
release any storage maintained by the channel driver for this channel, and
close the input and output devices encapsulated by this channel. All queued
output will have been flushed to the device before this function is called,
and no further driver operations will be invoked on this instance after
calling the <emphasis remap='I'>closeProc</emphasis>. If the close operation is successful, the
procedure should return zero; otherwise it should return a nonzero POSIX
error code. In addition, if an error occurs and <varname role='parameter'>interp</varname> is not NULL,
the procedure should store an error message in the interpreter's result.</para>

<para>Alternatively, channels that support closing the read and write sides
independently may set <emphasis remap='I'>closeProc</emphasis> to <emphasis remap='B'>TCL_CLOSE2PROC</emphasis> and set
<emphasis remap='I'>close2Proc</emphasis> to the address of a function that matches the
following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverClose2Proc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>Tcl_Interp * <parameter>interp</parameter></paramdef>
    <paramdef>int <parameter>flags</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para>The <emphasis remap='I'>close2Proc</emphasis> will be called with <varname role='parameter'>flags</varname> set to an OR'ed
combination of <emphasis remap='B'>TCL_CLOSE_READ</emphasis> or <emphasis remap='B'>TCL_CLOSE_WRITE</emphasis> to
indicate that the driver should close the read and/or write side of
the channel.  The channel driver may be invoked to perform
additional operations on the channel after <emphasis remap='I'>close2Proc</emphasis> is
called to close one or both sides of the channel.  If <varname role='parameter'>flags</varname> is
<literal>0</literal> (zero), the driver should close the channel in the manner
described above for <emphasis remap='I'>closeProc</emphasis>.  No further operations will be
invoked on this instance after <emphasis remap='I'>close2Proc</emphasis> is called with all
flags cleared.  In all cases, the <emphasis remap='I'>close2Proc</emphasis> function should
return zero if the close operation was successful; otherwise it should
return a nonzero POSIX error code. In addition, if an error occurs and
<varname role='parameter'>interp</varname> is not NULL, the procedure should store an error message
in the interpreter's result.</para>

<para>The <emphasis remap='I'>closeProc</emphasis> and <emphasis remap='I'>close2Proc</emphasis> values can be retrieved with
<emphasis remap='B'>Tcl_ChannelCloseProc</emphasis> or <emphasis remap='B'>Tcl_ChannelClose2Proc</emphasis>, which
return a pointer to the respective function.</para>
</refsect2>

<refsect2 xml:id='inputproc'><title>INPUTPROC</title>

<para>The <emphasis remap='I'>inputProc</emphasis> field contains the address of a function called by the
generic layer to read data from the file or device and store it in an
internal buffer. <emphasis remap='I'>InputProc</emphasis> must match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverInputProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>char * <parameter>buf</parameter></paramdef>
    <paramdef>int <parameter>bufSize</parameter></paramdef>
    <paramdef>int * <parameter>errorCodePtr</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para><emphasis remap='I'>InstanceData</emphasis> is the same as the value passed to
<function>Tcl_CreateChannel</function> when the channel was created.  The <varname role='parameter'>buf</varname>
argument points to an array of bytes in which to store input from the
device, and the <varname role='parameter'>bufSize</varname> argument indicates how many bytes are
available at <varname role='parameter'>buf</varname>.</para>

<para>The <varname role='parameter'>errorCodePtr</varname> argument points to an integer variable provided by
the generic layer. If an error occurs, the function should set the variable
to a POSIX error code that identifies the error that occurred.</para>

<para>The function should read data from the input device encapsulated by the
channel and store it at <varname role='parameter'>buf</varname>.  On success, the function should return
a nonnegative integer indicating how many bytes were read from the input
device and stored at <varname role='parameter'>buf</varname>. On error, the function should return -1. If
an error occurs after some data has been read from the device, that data is
lost.</para>

<para>If <emphasis remap='I'>inputProc</emphasis> can determine that the input device has some data
available but less than requested by the <varname role='parameter'>bufSize</varname> argument, the
function should only attempt to read as much data as is available and
return without blocking. If the input device has no data available
whatsoever and the channel is in nonblocking mode, the function should
return an <errorcode>EAGAIN</errorcode> error. If the input device has no data available
whatsoever and the channel is in blocking mode, the function should block
for the shortest possible time until at least one byte of data can be read
from the device; then, it should return as much data as it can read without
blocking.</para>

<para>This value can be retrieved with <emphasis remap='B'>Tcl_ChannelInputProc</emphasis>, which returns
a pointer to the function.</para>
</refsect2>

<refsect2 xml:id='outputproc'><title>OUTPUTPROC</title>

<para>The <emphasis remap='I'>outputProc</emphasis> field contains the address of a function called by the
generic layer to transfer data from an internal buffer to the output device.
<emphasis remap='I'>OutputProc</emphasis> must match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverOutputProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>const char * <parameter>buf</parameter></paramdef>
    <paramdef>int <parameter>toWrite</parameter></paramdef>
    <paramdef>int * <parameter>errorCodePtr</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para><emphasis remap='I'>InstanceData</emphasis> is the same as the value passed to
<function>Tcl_CreateChannel</function> when the channel was created. The <varname role='parameter'>buf</varname>
argument contains an array of bytes to be written to the device, and the
<varname role='parameter'>toWrite</varname> argument indicates how many bytes are to be written from the
<varname role='parameter'>buf</varname> argument.</para>

<para>The <varname role='parameter'>errorCodePtr</varname> argument points to an integer variable provided by
the generic layer. If an error occurs, the function should set this
variable to a POSIX error code that identifies the error.</para>

<para>The function should write the data at <varname role='parameter'>buf</varname> to the output device
encapsulated by the channel. On success, the function should return a
nonnegative integer indicating how many bytes were written to the output
device.  The return value is normally the same as <varname role='parameter'>toWrite</varname>, but may be
less in some cases such as if the output operation is interrupted by a
signal. If an error occurs the function should return -1.  In case of
error, some data may have been written to the device.</para>

<para>If the channel is nonblocking and the output device is unable to absorb any
data whatsoever, the function should return -1 with an <errorcode>EAGAIN</errorcode> error
without writing any data.</para>

<para>This value can be retrieved with <emphasis remap='B'>Tcl_ChannelOutputProc</emphasis>, which returns
a pointer to the function.</para>
</refsect2>

<refsect2 xml:id='seekproc_and_wideseekproc'><title>SEEKPROC AND WIDESEEKPROC</title>

<para>The <emphasis remap='I'>seekProc</emphasis> field contains the address of a function called by the
generic layer to move the access point at which subsequent input or output
operations will be applied. <emphasis remap='I'>SeekProc</emphasis> must match the following
prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverSeekProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>long <parameter>offset</parameter></paramdef>
    <paramdef>int <parameter>seekMode</parameter></paramdef>
    <paramdef>int * <parameter>errorCodePtr</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para>The <varname role='parameter'>instanceData</varname> argument is the same as the value given to
<function>Tcl_CreateChannel</function> when this channel was created.  <emphasis remap='I'>Offset</emphasis> and
<varname role='parameter'>seekMode</varname> have the same meaning as for the <emphasis remap='B'>Tcl_Seek</emphasis>
procedure (described in the manual entry for <emphasis remap='B'>Tcl_OpenFileChannel</emphasis>).</para>

<para>The <varname role='parameter'>errorCodePtr</varname> argument points to an integer variable provided by
the generic layer for returning <varname>errno</varname> values from the function.  The
function should set this variable to a POSIX error code if an error occurs.
The function should store an <errorcode>EINVAL</errorcode> error code if the channel type
does not implement seeking.</para>

<para>The return value is the new access point or -1 in case of error. If an
error occurred, the function should not move the access point.</para>

<para>If there is a non-NULL <emphasis remap='I'>seekProc</emphasis> field, the <emphasis remap='I'>wideSeekProc</emphasis>
field may contain the address of an alternative function to use which
handles wide (i.e. larger than 32-bit) offsets, so allowing seeks
within files larger than 2GB.  The <emphasis remap='I'>wideSeekProc</emphasis> will be called
in preference to the <emphasis remap='I'>seekProc</emphasis>, but both must be defined if the
<emphasis remap='I'>wideSeekProc</emphasis> is defined.  <emphasis remap='I'>WideSeekProc</emphasis> must match the
following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef Tcl_WideInt <function>Tcl_DriverWideSeekProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>Tcl_WideInt <parameter>offset</parameter></paramdef>
    <paramdef>int <parameter>seekMode</parameter></paramdef>
    <paramdef>int * <parameter>errorCodePtr</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para>The arguments and return values mean the same thing as with
<emphasis remap='I'>seekProc</emphasis> above, except that the type of offsets and the return
type are different.</para>

<para>The <emphasis remap='I'>seekProc</emphasis> value can be retrieved with
<emphasis remap='B'>Tcl_ChannelSeekProc</emphasis>, which returns a pointer to the function,
and similarly the <emphasis remap='I'>wideSeekProc</emphasis> can be retrieved with
<emphasis remap='B'>Tcl_ChannelWideSeekProc</emphasis>.</para>
</refsect2>

<refsect2 xml:id='setoptionproc'><title>SETOPTIONPROC</title>

<para>The <emphasis remap='I'>setOptionProc</emphasis> field contains the address of a function called by
the generic layer to set a channel type specific option on a channel.
<emphasis remap='I'>setOptionProc</emphasis> must match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverSetOptionProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>Tcl_Interp * <parameter>interp</parameter></paramdef>
    <paramdef>const char * <parameter>optionName</parameter></paramdef>
    <paramdef>const char * <parameter>newValue</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para><varname role='parameter'>optionName</varname> is the name of an option to set, and <varname role='parameter'>newValue</varname> is
the new value for that option, as a string. The <varname role='parameter'>instanceData</varname> is the
same as the value given to <function>Tcl_CreateChannel</function> when this channel was
created. The function should do whatever channel type specific action is
required to implement the new value of the option.</para>

<para>Some options are handled by the generic code and this function is never
called to set them, e.g. <option>-blockmode</option>. Other options are specific to
each channel type and the <emphasis remap='I'>setOptionProc</emphasis> procedure of the channel
driver will get called to implement them. The <emphasis remap='I'>setOptionProc</emphasis> field can
be NULL, which indicates that this channel type supports no type specific
options.</para> 

<para>If the option value is successfully modified to the new value, the function
returns <emphasis remap='B'>TCL_OK</emphasis>.
It should call <function>Tcl_BadChannelOption</function> which itself returns
<emphasis remap='B'>TCL_ERROR</emphasis> if the <varname role='parameter'>optionName</varname> is
unrecognized. 
If <varname role='parameter'>newValue</varname> specifies a value for the option that
is not supported or if a system call error occurs,
the function should leave an error message in the
<emphasis remap='I'>result</emphasis> field of <varname role='parameter'>interp</varname> if <varname role='parameter'>interp</varname> is not NULL. The
function should also call <emphasis remap='B'>Tcl_SetErrno</emphasis> to store an appropriate POSIX
error code.</para>

<para>This value can be retrieved with <emphasis remap='B'>Tcl_ChannelSetOptionProc</emphasis>, which returns
a pointer to the function.</para>
</refsect2>

<refsect2 xml:id='getoptionproc'><title>GETOPTIONPROC</title>

<para>The <emphasis remap='I'>getOptionProc</emphasis> field contains the address of a function called by
the generic layer to get the value of a channel type specific option on a
channel. <emphasis remap='I'>getOptionProc</emphasis> must match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverGetOptionProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>Tcl_Interp * <parameter>interp</parameter></paramdef>
    <paramdef>const char * <parameter>optionName</parameter></paramdef>
    <paramdef>Tcl_DString * <parameter>optionValue</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para><emphasis remap='I'>OptionName</emphasis> is the name of an option supported by this type of
channel. If the option name is not NULL, the function stores its current
value, as a string, in the Tcl dynamic string <varname role='parameter'>optionValue</varname>.
If <varname role='parameter'>optionName</varname> is NULL, the function stores in <varname role='parameter'>optionValue</varname> an
alternating list of all supported options and their current values.
On success, the function returns <emphasis remap='B'>TCL_OK</emphasis>. 
It should call <function>Tcl_BadChannelOption</function> which itself returns
<emphasis remap='B'>TCL_ERROR</emphasis> if the <varname role='parameter'>optionName</varname> is
unrecognized. If a system call error occurs,
the function should leave an error message in the
result of <varname role='parameter'>interp</varname> if <varname role='parameter'>interp</varname> is not NULL. The
function should also call <emphasis remap='B'>Tcl_SetErrno</emphasis> to store an appropriate POSIX
error code.</para>

<para>Some options are handled by the generic code and this function is never
called to retrieve their value, e.g. <option>-blockmode</option>. Other options are
specific to each channel type and the <emphasis remap='I'>getOptionProc</emphasis> procedure of the
channel driver will get called to implement them. The <emphasis remap='I'>getOptionProc</emphasis>
field can be NULL, which indicates that this channel type supports no type
specific options.</para>

<para>This value can be retrieved with <emphasis remap='B'>Tcl_ChannelGetOptionProc</emphasis>, which returns
a pointer to the function.</para>
</refsect2>

<refsect2 xml:id='watchproc'><title>WATCHPROC</title>

<para>The <emphasis remap='I'>watchProc</emphasis> field contains the address of a function called
by the generic layer to initialize the event notification mechanism to
notice events of interest on this channel.
<emphasis remap='I'>WatchProc</emphasis> should match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef void <function>Tcl_DriverWatchProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>int <parameter>mask</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para>The <varname role='parameter'>instanceData</varname> is the same as the value passed to
<function>Tcl_CreateChannel</function> when this channel was created. The <varname role='parameter'>mask</varname>
argument is an OR-ed combination of <emphasis remap='B'>TCL_READABLE</emphasis>, <emphasis remap='B'>TCL_WRITABLE</emphasis>
and <emphasis remap='B'>TCL_EXCEPTION</emphasis>; it indicates events the caller is interested in
noticing on this channel.</para>

<para>The function should initialize device type specific mechanisms to
notice when an event of interest is present on the channel.  When one
or more of the designated events occurs on the channel, the channel
driver is responsible for calling <function>Tcl_NotifyChannel</function> to inform
the generic channel module.  The driver should take care not to starve
other channel drivers or sources of callbacks by invoking
Tcl_NotifyChannel too frequently.  Fairness can be insured by using
the Tcl event queue to allow the channel event to be scheduled in sequence
with other events.  See the description of <emphasis remap='B'>Tcl_QueueEvent</emphasis> for
details on how to queue an event.</para>

<para>This value can be retrieved with <emphasis remap='B'>Tcl_ChannelWatchProc</emphasis>, which returns
a pointer to the function.</para>
</refsect2>

<refsect2 xml:id='gethandleproc'><title>GETHANDLEPROC</title>

<para>The <emphasis remap='I'>getHandleProc</emphasis> field contains the address of a function called by
the generic layer to retrieve a device-specific handle from the channel.
<emphasis remap='I'>GetHandleProc</emphasis> should match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverGetHandleProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>int <parameter>direction</parameter></paramdef>
    <paramdef>ClientData * <parameter>handlePtr</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para><emphasis remap='I'>InstanceData</emphasis> is the same as the value passed to
<function>Tcl_CreateChannel</function> when this channel was created. The <varname role='parameter'>direction</varname>
argument is either <emphasis remap='B'>TCL_READABLE</emphasis> to retrieve the handle used
for input, or <emphasis remap='B'>TCL_WRITABLE</emphasis> to retrieve the handle used for
output.</para>

<para>If the channel implementation has device-specific handles, the
function should retrieve the appropriate handle associated with the
channel, according the <varname role='parameter'>direction</varname> argument.  The handle should be
stored in the location referred to by <varname role='parameter'>handlePtr</varname>, and
<emphasis remap='B'>TCL_OK</emphasis> should be returned.  If the channel is not open for the
specified direction, or if the channel implementation does not use
device handles, the function should return <emphasis remap='B'>TCL_ERROR</emphasis>.</para>

<para>This value can be retrieved with <emphasis remap='B'>Tcl_ChannelGetHandleProc</emphasis>, which returns
a pointer to the function.</para>
</refsect2>

<refsect2 xml:id='flushproc'><title>FLUSHPROC</title>

<para>The <emphasis remap='I'>flushProc</emphasis> field is currently reserved for future use.
It should be set to NULL.
<emphasis remap='I'>FlushProc</emphasis> should match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverFlushProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para>This value can be retrieved with <emphasis remap='B'>Tcl_ChannelFlushProc</emphasis>, which returns
a pointer to the function.</para>
</refsect2>

<refsect2 xml:id='handlerproc'><title>HANDLERPROC</title>

<para>The <emphasis remap='I'>handlerProc</emphasis> field contains the address of a function called by
the generic layer to notify the channel that an event occurred.  It should
be defined for stacked channel drivers that wish to be notified of events
that occur on the underlying (stacked) channel.
<emphasis remap='I'>HandlerProc</emphasis> should match the following prototype:</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverHandlerProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>int <parameter>interestMask</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para><emphasis remap='I'>InstanceData</emphasis> is the same as the value passed to <function>Tcl_CreateChannel</function>
when this channel was created.  The <varname role='parameter'>interestMask</varname> is an OR-ed
combination of <emphasis remap='B'>TCL_READABLE</emphasis> or <emphasis remap='B'>TCL_WRITABLE</emphasis>; it indicates what
type of event occurred on this channel.</para>

<para>This value can be retrieved with <emphasis remap='B'>Tcl_ChannelHandlerProc</emphasis>, which returns
a pointer to the function.</para>

</refsect2>

<refsect2 xml:id='threadactionproc'><title>THREADACTIONPROC</title>

<para>The <emphasis remap='I'>threadActionProc</emphasis> field contains the address of the function
called by the generic layer when a channel is created, closed, or
going to move to a different thread, i.e. whenever thread-specific
driver state might have to initialized or updated. It can be NULL.
The action <emphasis remap='I'>TCL_CHANNEL_THREAD_REMOVE</emphasis> is used to notify the
driver that it should update or remove any thread-specific data it
might be maintaining for the channel.</para>

<para>The action <emphasis remap='I'>TCL_CHANNEL_THREAD_INSERT</emphasis> is used to notify the
driver that it should update or initialize any thread-specific data it
might be maintaining using the calling thread as the associate. See
<function>Tcl_CutChannel</function> and <function>Tcl_SpliceChannel</function> for more detail.</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef void <function>Tcl_DriverThreadActionProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>int <parameter>action</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para><emphasis remap='I'>InstanceData</emphasis> is the same as the value passed to
<function>Tcl_CreateChannel</function> when this channel was created.</para>

<para>These values can be retrieved with <emphasis remap='B'>Tcl_ChannelThreadActionProc</emphasis>,
which returns a pointer to the function.</para>
</refsect2>

<refsect2 xml:id='truncateproc'><title>TRUNCATEPROC</title>

<para>The <emphasis remap='I'>truncateProc</emphasis> field contains the address of the function
called by the generic layer when a channel is truncated to some
length. It can be NULL.</para>

<programlisting remap='CS'>
<funcsynopsis>
<funcprototype>
<funcdef>typedef int <function>Tcl_DriverTruncateProc</function></funcdef>
    <paramdef>ClientData <parameter>instanceData</parameter></paramdef>
    <paramdef>Tcl_WideInt <parameter>length</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</programlisting> <!-- remap='CE' -->
<para><emphasis remap='I'>InstanceData</emphasis> is the same as the value passed to
<function>Tcl_CreateChannel</function> when this channel was created, and
<varname role='parameter'>length</varname> is the new length of the underlying file, which should
not be negative. The result should be 0 on success or an errno code
(suitable for use with <emphasis remap='B'>Tcl_SetErrno</emphasis>) on failure.</para>

<para>These values can be retrieved with <emphasis remap='B'>Tcl_ChannelTruncateProc</emphasis>,
which returns a pointer to the function.</para>
</refsect2>
</refsect1>

<refsect1 xml:id='tcl_badchanneloption'><title>TCL_BADCHANNELOPTION</title>
<para>This procedure generates a
<quote>bad option</quote>
error message in an
(optional) interpreter.  It is used by channel drivers when 
an invalid Set/Get option is requested. Its purpose is to concatenate
the generic options list to the specific ones and factorize
the generic options error message string.</para>

<para>It always returns <emphasis remap='B'>TCL_ERROR</emphasis></para>

<para>An error message is generated in <varname role='parameter'>interp</varname>'s result object to
indicate that a command was invoked with a bad option.
The message has the form</para>
<programlisting remap='CS'>
    bad option "blah": should be one of 
    &lt;...generic options...&gt;+&lt;...specific options...&gt;
</programlisting> <!-- remap='CE' -->
<para>so you get for instance:</para>
<programlisting remap='CS'>
    bad option "-blah": should be one of -blocking,
    -buffering, -buffersize, -eofchar, -translation,
    -peername, or -sockname
</programlisting> <!-- remap='CE' -->
<para>when called with <emphasis remap='I'>optionList</emphasis> equal to
<quote>peername sockname</quote>

<quote>blah</quote></para>
<para>is the <varname role='parameter'>optionName</varname> argument and
<quote>&lt;specific options&gt;</quote>
is a space separated list of specific option words.
The function takes good care of inserting minus signs before
each option, commas after, and an
<quote>or</quote>
before the last option.</para>
</refsect1>

<refsect1 xml:id='old_channel_types'><title>OLD CHANNEL TYPES</title>
<para>The original (8.3.1 and below) <function>Tcl_ChannelType</function> structure contains
the following fields:</para>

<programlisting remap='CS'>
typedef struct Tcl_ChannelType {
        char *<emphasis remap='I'>typeName</emphasis>;
        Tcl_DriverBlockModeProc *<emphasis remap='I'>blockModeProc</emphasis>;
        Tcl_DriverCloseProc *<emphasis remap='I'>closeProc</emphasis>;
        Tcl_DriverInputProc *<emphasis remap='I'>inputProc</emphasis>;
        Tcl_DriverOutputProc *<emphasis remap='I'>outputProc</emphasis>;
        Tcl_DriverSeekProc *<emphasis remap='I'>seekProc</emphasis>;
        Tcl_DriverSetOptionProc *<emphasis remap='I'>setOptionProc</emphasis>;
        Tcl_DriverGetOptionProc *<emphasis remap='I'>getOptionProc</emphasis>;
        Tcl_DriverWatchProc *<emphasis remap='I'>watchProc</emphasis>;
        Tcl_DriverGetHandleProc *<emphasis remap='I'>getHandleProc</emphasis>;
        Tcl_DriverClose2Proc *<emphasis remap='I'>close2Proc</emphasis>;
} Tcl_ChannelType;
</programlisting> <!-- remap='CE' -->

<para>It is still possible to create channel with the above structure.  The
internal channel code will determine the version.  It is imperative to use
the new <function>Tcl_ChannelType</function> structure if you are creating a stacked
channel driver, due to problems with the earlier stacked channel
implementation (in 8.2.0 to 8.3.1).</para>

<para>Prior to 8.4.0 (i.e. during the later releases of 8.3 and early part
of the 8.4 development cycle) the <function>Tcl_ChannelType</function> structure
contained the following fields:</para>

<programlisting remap='CS'>
typedef struct Tcl_ChannelType {
        char *<emphasis remap='I'>typeName</emphasis>;
        Tcl_ChannelTypeVersion <emphasis remap='I'>version</emphasis>;
        Tcl_DriverCloseProc *<emphasis remap='I'>closeProc</emphasis>;
        Tcl_DriverInputProc *<emphasis remap='I'>inputProc</emphasis>;
        Tcl_DriverOutputProc *<emphasis remap='I'>outputProc</emphasis>;
        Tcl_DriverSeekProc *<emphasis remap='I'>seekProc</emphasis>;
        Tcl_DriverSetOptionProc *<emphasis remap='I'>setOptionProc</emphasis>;
        Tcl_DriverGetOptionProc *<emphasis remap='I'>getOptionProc</emphasis>;
        Tcl_DriverWatchProc *<emphasis remap='I'>watchProc</emphasis>;
        Tcl_DriverGetHandleProc *<emphasis remap='I'>getHandleProc</emphasis>;
        Tcl_DriverClose2Proc *<emphasis remap='I'>close2Proc</emphasis>;
        Tcl_DriverBlockModeProc *<emphasis remap='I'>blockModeProc</emphasis>;
        Tcl_DriverFlushProc *<emphasis remap='I'>flushProc</emphasis>;
        Tcl_DriverHandlerProc *<emphasis remap='I'>handlerProc</emphasis>;
        Tcl_DriverTruncateProc *<emphasis remap='I'>truncateProc</emphasis>;
} Tcl_ChannelType;
</programlisting> <!-- remap='CE' -->

<para>When the above structure is registered as a channel type, the
<emphasis remap='I'>version</emphasis> field should always be <emphasis remap='B'>TCL_CHANNEL_VERSION_2</emphasis>.</para>

</refsect1>

<refsect1 xml:id='see_also'><title>SEE ALSO</title>
<para>Tcl_C<citerefentry><refentrytitle>lose</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_OpenFileC<citerefentry><refentrytitle>hannel</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_SetE<citerefentry><refentrytitle>rrno</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_QueueE<citerefentry><refentrytitle>vent</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_StackC<citerefentry><refentrytitle>hannel</refentrytitle><manvolnum>3</manvolnum></citerefentry>, Tcl_GetStdC<citerefentry><refentrytitle>hannel</refentrytitle><manvolnum>3</manvolnum></citerefentry></para>

</refsect1>

<refsect1 xml:id='keywords'><title>KEYWORDS</title>
<para>blocking, channel driver, channel registration, channel type, nonblocking</para>
</refsect1>
</refentry>

