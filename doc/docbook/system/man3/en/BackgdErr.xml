<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man+tkman by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='backgderr3'>

<!-- '\" Copyright (c) 1992\-1994 The Regents of the University of California.
'\" Copyright (c) 1994\-1996 Sun Microsystems, Inc. -->

<!-- '\" See the file "license.terms" for information on usage and redistribution
'\" of this file, and for a DISCLAIMER OF ALL WARRANTIES.
'\" 
'\" RCS: @(#) $Id$
'\"  -->
<refentryinfo><date>7.5</date></refentryinfo>
<refmeta>
<refentrytitle>Tcl_BackgroundError</refentrytitle>
<manvolnum>3</manvolnum>
<refmiscinfo class='date'>7.5</refmiscinfo>
<refmiscinfo class='source'>Tcl</refmiscinfo>
<refmiscinfo class='manual'>Tcl Library Procedures</refmiscinfo>
</refmeta>
<refnamediv>
<refname>Tcl_BackgroundError</refname>
<refpurpose>report Tcl error that occurred in background processing</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<funcsynopsis>
<funcsynopsisinfo>
#include &lt;tcl.h&gt;

</funcsynopsisinfo>
<funcprototype>
<funcdef><function>Tcl_BackgroundError</function></funcdef>
<paramdef><parameter>interp</parameter></paramdef>
</funcprototype>
</funcsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='arguments'><title>ARGUMENTS</title>
<!-- .AS Tcl_Interp *interp -->
<informaltable>
<tgroup cols='3'>
<tbody>

<row><entry>"Tcl_Interp"</entry><entry>"*interp"</entry><entry>"in"</entry>
<entry>
<para>Interpreter in which the error occurred.</para>
<!-- .BE -->
</entry>
</row>
</tbody>
</tgroup>
</informaltable>

</refsect1>

<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para>This procedure is typically invoked when a Tcl error occurs during
<quote>background processing</quote>
such as executing an event handler.
When such an error occurs, the error condition is reported to Tcl
or to a widget or some other C code, and there is not usually any
obvious way for that code to report the error to the user.
In these cases the code calls <function>Tcl_BackgroundError</function> with an
<emphasis remap='I'>interp</emphasis> argument identifying the interpreter in which the
error occurred.  At the time <function>Tcl_BackgroundError</function> is invoked,
the interpreter's result is expected to contain an error message.
<function>Tcl_BackgroundError</function> will invoke the command registered
in that interpreter to handle background errors by the
<emphasis remap='B'>interp bgerror</emphasis> command.
The registered handler command is meant to report the error
in an application-specific fashion.  The handler command
receives two arguments, the result of the interp, and the
return options of the interp at the time the error occurred.
If the application registers no handler command, the default
handler command will attempt to call <emphasis remap='B'>bgerror</emphasis> to report
the error.  If an error condition arises while invoking the
handler command, then <function>Tcl_BackgroundError</function> reports the
error itself by printing a message on the standard error file.</para>

<para><function>Tcl_BackgroundError</function> does not invoke the handler command immediately
because this could potentially interfere with scripts that are in process
at the time the error occurred.
Instead, it invokes the handler command later as an idle callback.</para>

<para>It is possible for many background errors to accumulate before
the handler command is invoked.  When this happens, each of the errors
is processed in order.  However, if the handle command returns a
break exception, then all remaining error reports for the
interpreter are skipped.</para>

</refsect1>

<refsect1 xml:id='keywords'><title>KEYWORDS</title>
<para>background, bgerror, error, interp</para>
</refsect1>
</refentry>

