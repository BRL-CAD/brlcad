<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- lifted from troff+man by doclifter -->
<refentry xmlns='http://docbook.org/ns/docbook' version='5.0' xml:lang='en' xml:id='hilbert3'>
<!--  Copyright 1991, The Regents of the University of Michigan -->
<refentryinfo><date>3/12/91</date></refentryinfo>
<refmeta>
<refentrytitle>HILBERT</refentrytitle>
<manvolnum>3</manvolnum>
<refmiscinfo class='date'>3/12/91</refmiscinfo>
<refmiscinfo class='source'>3</refmiscinfo>
</refmeta>
<refnamediv>
<refname>hilbert_i2c</refname>
<refname>hilbert_c2i</refname>
<refpurpose>Compute points on a Hilbert curve.</refpurpose>
</refnamediv>
<!-- body begins here -->
<refsynopsisdiv xml:id='synopsis'>
<funcsynopsis>
<funcprototype>
<funcdef>void <function>hilbert_i2c</function></funcdef>
<paramdef><parameter>dim</parameter></paramdef>
<paramdef><parameter>bits</parameter></paramdef>
<paramdef><parameter>idx</parameter></paramdef>
<paramdef><parameter>coords</parameter></paramdef>
</funcprototype>
<funcsynopsisinfo>

int dim, bits;

long int idx;

int coords[];

</funcsynopsisinfo>
<funcprototype>
<funcdef>void <function>hilbert_c2i</function></funcdef>
<paramdef><parameter>dim</parameter></paramdef>
<paramdef><parameter>bits</parameter></paramdef>
<paramdef><parameter>coords</parameter></paramdef>
<paramdef><parameter>idx</parameter></paramdef>
</funcprototype>
<funcsynopsisinfo>

int dim, bits;

int coords[];

long int *idx;
</funcsynopsisinfo>
</funcsynopsis>
</refsynopsisdiv>


<refsect1 xml:id='description'><title>DESCRIPTION</title>
<para>These procedures map the real line onto a Hilbert curve and vice
versa.  (A Hilbert curve is a space filling curve similar to the Peano
curve, except it is not closed.)  The procedure <function>hilbert_i2c</function>
returns the coordinates of a point on the Hilbert curve, given an
index value representing its sequential position on the curve.  The
procedure <function>hilbert_c2i</function> reverses the process.  The arguments are:</para>
<variablelist remap='TP'>
  <varlistentry>
  <term><emphasis remap='I'>dim</emphasis></term>
  <listitem>
<para>The dimensionality of the Hilbert curve.  For the usual planar curve
case, this would be 2.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='I'>bits</emphasis></term>
  <listitem>
<para>The resolution to which the Hilbert curve will be computed.  The space
is quantized to 2^<emphasis remap='I'>bits</emphasis> values on each axis, so there are
2^(3*<emphasis remap='I'>bits</emphasis>) points on the curve.  The product of
<emphasis remap='I'>dim</emphasis>*<emphasis remap='I'>bits</emphasis> should be less than or equal to the number of
bits in a long integer (typically 32), and <emphasis remap='I'>bits</emphasis> should be less
than or equal to the number of bits in an integer.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='I'>idx</emphasis></term>
  <listitem>
<para>The sequential position of the point along the curve (starting from
0).  This is a 3*<emphasis remap='I'>bits</emphasis> bit integer.</para>
  </listitem>
  </varlistentry>
  <varlistentry>
  <term><emphasis remap='I'>coords</emphasis></term>
  <listitem>
<para>The spatial coordinates of the point on the curve.  The array should
hold <emphasis remap='I'>dim</emphasis> values.  Each is a <emphasis remap='I'>bits</emphasis> bit integer.</para>
  </listitem>
  </varlistentry>
</variablelist>
</refsect1>

<refsect1 xml:id='reference'><title>REFERENCE</title>
<para>A. R. Butz, "Alternative algorithm for Hilbert's space-filling
curve," <emphasis remap='I'>IEEE Trans. Comput.</emphasis>, vol C-20, pp. 424-426, Apr. 1971.</para>
</refsect1>

<refsect1 xml:id='author'><title>AUTHOR</title>
<para>Spencer W. Thomas</para>
</refsect1>
</refentry>

