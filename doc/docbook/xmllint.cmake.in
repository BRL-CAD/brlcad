execute_process(COMMAND "@XMLLINT_EXECUTABLE@" @XMLLINT_FLAGS@ @CMAKE_CURRENT_SOURCE_DIR@/@filename@ RESULT_VARIABLE CMDRESULT)
IF(CMDRESULT)
	MESSAGE(FATAL_ERROR "xmllint failure: ${CMDRESULT}")
ELSE(CMDRESULT)
	execute_process(COMMAND @CMAKE_COMMAND@ -E touch @db_outfile@)
ENDIF(CMDRESULT)

