/*
 *			doc/Cakefile
 */
#include "../Cakefile.defs"

#define PRODUCTS	/**/
#define SRCDIR	doc
#define FILES	/**/
#define	SRCSUFF	.doc
#define MANSECTION	5

/*
 *  There is nothing to compile.  The "inst-dist" rules should take
 *  all the documents and send them to distribution directory,
 *  on the master machine.  Otherwise, do nothing.
 */

all&:
	@echo "doc:  all done"

#include "../Cakefile.rules"
