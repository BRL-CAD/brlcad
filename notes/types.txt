stdin/stdout redirected
  input ascii
    a-d
  input alias
    alias-pix
  input custom ascii
    anim_cascade
      waits for stdin when no args are given
    
  
argument given
  input geometry
    all_sf
